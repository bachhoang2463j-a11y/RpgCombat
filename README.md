# 战斗前端系统测试说明文档 (完整版)

**当前版本：V10.7**

> V10.7：战后数值直连 MiniMapStatus 状态栏——结算数值（HP/MP/存活/消耗品弹药余量）经聊天变量 `$rpg_combat_result` 程序写入状态栏（0 次 LLM 转述），提示词改为程序换算的伤势档位清单，结算弹窗新增同步状态徽标；状态栏侧带合并层硬锁防二次扣减。详见 §10.4 与 LOG-196。
> V10.6：`[暴击:N%]` / `[吸蓝;power:N]` 新标签（敌我/技能/物品通用）+ 反应/看破/舍身三路径绑定物品消耗闭环（候选预筛、弹窗徽章、战报消耗行与触发来源）+ 效果槽解析加固（跨括号吞并修复、`[标签:N]` 冒号简写归一化）。详见 LOG-191。
> V10.5：物品道具与弹药消耗系统（消耗品/道具/弹药三类、技能材料绑定、高阶/传奇物品罕见旁白、战后全量结算与编辑器物品配置）+ 主行动菜单四模块重构（基础行动/攻击技能/辅助技能/物品道具）+ 战局记录纯 CSS 回合折叠 + 对话设置自定义末尾注入模块（思考阻断）。详见 LOG-185~190。

本战斗前端采用**纯本地数值与逻辑运算**！所有的伤害计算、命中闪避、技能判定、敌方AI决策等都由前端代码瞬间完成，不需要等待 AI 思考。LLM（大语言模型）**只负责在战斗回合之间生成角色的对话（台词旁白）以及战后/撤退总结**，这极大地保证了战斗的流畅性与操作即时性。

---

## 1. 战局数据交互与 `<Combat_block>` 格式

当战斗开始或需要同步初始数据时，前端会通过正则检测酒馆聊天记录（SillyTavern 上下文）中是否包含 `<Combat_block>` 标签。

### AI 或系统提示词中需要包含的格式模板：
大模型（或系统预设角色卡）需要在文本中输出以下格式的 YAML 数据，注意必须用 `<Combat_block>` 和 `</Combat_block>` 包裹：

```html
<Combat_block>
战斗数据栏:
  我方列表(场地:有备而来):
    - 角色:
        名字: "烈阳剑尊"
        属性: "[HP:100/100][MP:50/50][Atk:15][Armor:5][Speed:110][职业:防守者]"
        物品:
          - "【芭斯特的猫眼石】[道具][高阶][单回;power:50][次数:3][他人]"
          - "【战斗兴奋剂】[消耗品][单回;power:60][单冲;power:20][数量:3][他人]"
          - "【.45手枪子弹】[弹药][数量:45][绑定:柯尔特左轮三连发*3;快速点射*1]"
        技能:
          - "【赤阳剑气】[MP:10][单体;power:40][近战][特效:火焰01]"
          - "【烈阳守护】[MP:15][单防;power:20][持续:3]"
  敌方列表:
    - 角色:
        名字: "青眼苍狼"
        属性: "[HP:120/120][MP:30/30][Atk:12][Armor:3][Speed:95]"
        技能:
          - "【撕咬】[单体;power:25][近战]"
</Combat_block>
```

**解析规则说明：**
- **属性字段格式**：必须为 `[HP:当前/最大][MP:当前/最大][Atk:攻击力][Armor:护甲/防御][Speed:速度][职业:职业名]`；可选字段 `[行动次数:N]`（或 `[Act:N]`，如 `[行动次数:2]`）表示该角色每回合可行动 N 次，**我方与敌方通用**（V6.27），详见 §4.2。
- **物品字段格式（V10.5 可选）**：角色下属可选 `物品:` 列表（与 `技能:` 平级），格式为 `【物品名】[类别][效果槽;power:数值][数量:N 或 次数:N][绑定:技能名*消耗]`，支持 **`[消耗品]`**（跨场累计扣减）、**`[道具]`**（每场重置可用次数）、**`[弹药]`**（供绑定技能扣减材料）。详见 §10。
- **技能字段格式**：必须为 `【技能名】[消耗类型:数值][标签;power:数值][伤害类型][特效:特效名]` 等组合。
- **场地字段（V6.14 可选）**：`我方列表` 行可带后缀 `(场地:场地名)`（如 `我方列表(场地:有备而来):`），冒号支持半角 `:` 与全角 `：`。带场地时战斗开始前会为我方全体注入对应场地效果；**不带场地标签则无任何场地效果**；未注册的场地名会被忽略并输出控制台警告（战斗正常建立）。场地效果**仅作用于我方**，敌方不受影响。详见 §3.8。
- **敌方名字 `*N` 数量后缀（V6.15）**：敌方名字末尾加 `*N`（如 `名字: "❄️《冰雪随从》*2"`）表示一次载入 N 个同名敌人，半角 `*` 与全角 `×` 均可，仅敌方列表生效（我方靠编辑器/持久化维护）。配合世界书图鉴可一次点名多个同类敌人，详见 §9。
- **召唤技能 `[召唤:词条名]`（V10.2）**：技能串写法 `【技能名】[召唤:词条名][MP:N]…` 按世界书图鉴匹配敌人并**即时参战**（不再下回合延迟），由 `parseEnemyItem/getEnemyTier/assignEnemyRows` 同管线生成，继承词条被动与变种特性并走 `reorderRemainingQueue` 入队。详见 §3.11。
- **敌方名字变种标签 `[...]`（V6.25）**：敌方名字后可直接跟一个或多个被 `[]` 包裹的变种标签（如 `名字: "食尸鬼*2[复活][智慧]"`），世界书模式下属性由图鉴词条提供、名字标签照样生效（LLM 只需输出敌人名字+变种标签即可）。**双写入点**：变种标签既可写在名字后，也可写在属性栏（如 `属性: "[HP:100/100][复活][智慧]"`），两处完全等价、任选其一。已支持 19 个标签见 §3.9：**`[复活]`**（死亡后 30% 血量复活一次，仅一次）、**`[智慧]`**（残血集火AI+智慧旁白）、**`[流体]`/`[虚形]`/`[破法]`**（远程/近战/法术伤害 -50%）、**`[要害]`**（受群攻伤害 -50%）、**`[防火]`/`[耐毒]`/`[钢体]`**（免疫点燃/中毒/眩晕）、**`[再生]`**（每回合回血 10%）、**`[狂暴]`**（残血攻击 +50%）、**`[吸血]`**（攻击吸血 30%）、**`[自爆:群伤/群火/群毒/群穿]`**（死亡自爆，可带 `;power:N`）、**`[蜕皮]`**（首次跌破50%清debuff+回血30%+永久攻击+50%）、**`[荆棘]`**（近战命中反弹50%实际伤害，护甲减免后保底10）、**`[连动]`**（HP≤30%时立刻行动一次）。同一列表中「同名不同变种」敌人（如【食尸鬼】与【食尸鬼[复活]】）可安全共存。

---

## 2. 界面与 UI 系统介绍

### 2.1 对话界面与设置面板
- **“💬 对话”设置**：位于控制区，点击可配置 API 接口。
  - **API URL / Key / Model**：设置 LLM 接口，推荐使用能力强的模型（如 GPT-4o、Claude 3.5 Sonnet 或 Gemini 1.5 Pro）。
  - **传入酒馆聊天上下文**：开启后，系统在调用 LLM 时会抓取并附带 SillyTavern 的当前聊天历史，让角色的对话旁白能完美契合当前的故事线。
  - **上下文楼层数**：0 表示仅传入本次战局的日志，N 表示额外传入最近 N 层的聊天上下文。
  - **战局记录条数**：可在 `10` 到 `无限` 之间自由调节，决定发送给大模型进行判定的战斗历史记录条数。
  - **max_tokens（最大输出）**（V7.7）：控制每次 LLM 请求的最大输出 token 数（默认 4096，可调 256~8192）。**思考型模型（如 gemini-3-flash-preview）的思考 token 计入该上限**——经 OpenAI 兼容代理转发时 `max_tokens` 映射为 Gemini 的 `maxOutputTokens`，预算过低会被思考耗尽导致回应被截断（表现即台词输出不完整/丢失）。调高即可解决；`finish_reason='length'` 时面板会提示「⚠️ AI 回应被截断」。
  - **🧠 关闭思考（Thinking）**（V7.7）：勾选后请求体附加 `thinking: false`，尝试让思考型模型跳过思考直接输出正文。**仅部分 OpenAI 兼容代理透传该字段**，若代理不支持则勾选可能报错或无效，取消勾选即可（多数代理对未知字段静默忽略、无害）。
  - **🎯 末尾注入（思考阻断/补充要求）**（V10.5）：勾选启用后，以「用户」角色将自定义文本追加在 LLM API 请求的最末端（如放置 `<think>\nResponding budget exceeded.\n</think>` 思考阻断块以强制模型跳过思考链加速回复，或放置格式约束补充要求）。随 5 套预设独立存储并持久化。
  - **🎭 角色人设提示词**（V10.3）：`扮演要求` 后的 `details` 折叠块，默认展开、`N 条` 徽章与 `＋ 添加角色提示词` 入口；每条按角色名精确匹配当前出战阵容（与持久化 `rpg_combat_roster` 一致），仅对出战角色增量追加 `【出战角色补充人设】` 到 `systemContent`，未出战/临时队友不注入、仅走全局扮演要求；支持 5 预设槽位独立存储与 `characterPrompts` 迁移。
- **“友方旁白 ON/OFF”**：开启后，我方角色会在多个时机自动触发 LLM 生成旁白气泡（V6.28）：
  - **英雄行动时**：除 1 号位（玩家）之外的我方英雄行动（技能/普攻）后，以本人口吻说一句符合其最新动作的台词（原有）；
  - **被治疗时**：我方角色被【单回】/【群回】治疗并实际回血后说话——**一次 LLM 调用合并治疗者与被治疗者台词**（群回一次常多人回血，合并调用节省 token）：治疗者体现施术意图与关怀（1 号位作治疗者不自动发言），被治疗者表达伤势缓解的回应（含 1 号位）；
  - **力竭倒下**：HP 归 0 倒下时留下临别台词（如不甘、遗言、痛呼或诀别），含 1 号位；
  - **触发保命被动**：遭受致命攻击触发【毅力留存】（防守者/狂战士）或【回避致命】（风行者/隐匿者）死里逃生时，以本人口吻说话（如死里逃生的喘息、强撑的倔强或后怕），含 1 号位。
- **“敌方旁白 ON/OFF”**：开启后，带 **`[智慧]`** 标签的敌方角色会在三个时机自动触发 LLM 旁白（V6.26）：
  - **回合开始**：该敌人行动回合开始时以本人口吻说一句当前战局台词（原有智慧旁白）；
  - **生命跌破 30%**：生命首次跌至 30% 以下时以本人口吻说一句绝境台词（如负隅顽抗、绝望嘶吼或放狠话），**每次跌破 30% 仅触发一次**（回血后再跌破不重复触发）；
  - **被击杀**：被击败时留下最后一句临死台词（**`[复活]`** 敌人第一次被击杀复活不触发、复活后再死正常触发）。
  - 同名敌人（如 `食尸鬼*2[智慧]`）各实例独立说话，气泡归属各自实体，不会串到第一个同名敌人头上。
- **对话输入框**：游戏开始后，底部会亮起对话栏，输入文字并发送，场上的友方角色会根据当前的战况和您的发言自动进行群聊气泡回应。

### 2.2 战斗面板 UI
- **时间轴 (Turn Queue)（V7.5 动效全面升级）**：界面左侧竖向显示当前的行动顺序图标，排在最顶部的为当前行动者。
  - **崩铁原味平滑轮转**：行动切换时视觉队列整体以 0.6s 物理弹簧（FLIP + `cubic-bezier(0.34, 1.45, 0.64, 1)`）平滑上移，已行动者滑向队尾，新接管行动权的角色触发金色聚焦冲击环（`queue-focus-surge`）。
  - **两阶段再动爆闪**：再动/多重施法/隐匿者/祈愿/连动等插队项入场时，第一阶段携带金色雷光冲击波（`lightning-shockwave-ring`）与爆闪破空入队并常驻精致「⚡再动」琥珀流金置顶呼吸徽章（`extra-badge-active`），第二阶段自然平滑切换为当前行动者。
  - **赛博光轨回合光晕**：每轮大回合开始时，全队行动条图标按阵营色（我方青 `rgba(6,182,212,0.85)` / 敌方玫红 `rgba(244,63,94,0.85)` / 再动金 `rgba(251,191,36,0.9)`）错峰（逐项 40ms）发出 0.6s 漫射光晕（`queue-round-glow`）。
  - **串流能量导轨**：时间轴后方常驻纵向青金渐变导轨（`timeline-rail`）与 2.6s 循环流光粒子脉冲（`rail-pulse-beam`）。
  - **变速角标与浮动数值**：施放 `[单速]/[群速]/[单缓]/[群缓]` 触发排轴实时重排时，变动单位右上角弹出 **🟢 ▲ 翡翠绿箭头** + `+N速` 或 **🔴 ▼ 绯红红箭头** + `-N速` 动态角标与漂浮数值。
  - **响应式序号徽章**：左下角挂载精致 `#1~#N` 紧凑序号徽章（`rankPill`），横竖屏自动收敛至精细比例（`w-8 h-8 sm:w-10 sm:h-10`）。
- **角色卡牌**：
  - **TP（潜能槽）**：受到伤害（被命中 +10）、施放技能（+5）、**成功闪避（+5）**、普攻与防御姿态时积攒。攒满 100 时会高亮闪烁，此时点击头像可激活“极限爆发”（双倍技能效果，技能施放时 TP 清零），再次点击可收敛。**激活爆发后，本次施放的攻击与技能附加【必中】效果**（伤害与敌方减益恒命中，镜像 `[必中]` 标签语义：仍可被反应技/看破主动化解；仅我方生效，敌方满 TP 自动爆发不受影响）。激活爆发时，消耗 TP 的技能会被自动禁用并显示 `⚠️ 爆发中禁 NTP`（避免因清零导致释放条件不足失败），点击头像切换爆发时技能面板会自动实时刷新。爆发与 `[暴击]` **互斥**：爆发回合直接按 ×2 结算，不再掷暴击骰（不会 ×4），详见 §3.4。
  - **护盾条**：灰白色能量条覆盖在 HP 条上，优先吸收物理和法术伤害。
- **🛡️ 圣域帷幕与【肃正】全队共享屏障（V7.6）**：
  我方释放带有 `[肃正]` 标签的技能时，会为全队生成笼罩小队的 3D 弧形圣域结界屏障（`teamBarrier`），在小队顶部独立显示进度条与数值。
  - **固定耐久上限（V7.6）**：每次施放以本次技能 `power × 倍率` 为耐久上限（覆盖式全新创建，不再无限叠加），屏障破碎时耐久上限与护甲/克制属性随之清零重置，确保下次创建分母精确。
  - **屏障存在时灰卡禁用（V7.6）**：屏障存在期间（`teamBarrier > 0`），所有携带【肃正】标签的技能在技能面板自动灰卡禁用（显示 `⚠️ 屏障存在中`，点击无响应），蓄力入口亦被同步拦截，击破后方可重新施放。
  - **屏障类型配色（V7.6）**：根据屏障克制属性动态切换视觉主题——无属性/近战为**银白系**、远程为**科幻青蓝系**、法术为**金曜系**（CSS 变量动态驱动网格、光晕、边框及徽章）。
  - **屏障名字跟随技能名**：屏障横幅/战报/飘字一律使用施放该屏障的技能名（如 `【魔法壁垒】[肃正;power:1000]` 施放后屏障即显示为"魔法壁垒"，而非固定的"圣域帷幕"）。
  - **`Armor:N` 屏障护甲**：语法 `[肃正;power:1000;Armor:40]`，屏障对所有伤害（**含穿透**）拥有 N 点护甲值，伤害进入屏障吸收前**先扣减屏障护甲**；屏障被击破后护甲随之消散重置。
  - **子类型属性克制**：语法 `[肃正:法术;power:1000;Armor:40]`（子类型取 `近战`/`远程`/`法术`，也可用独立 `[法术]`/`[近战]`/`[远程]` 标签代替），屏障对该属性伤害额外 **50% 减免**，减免发生在护甲扣除之前（先减半 → 再扣护甲 → 剩余量进屏障吸收）。
  - **伪实体肉盾**：屏障存在期间，敌方单体攻击强制归集命中屏障伪目标，我方英雄完全不被选中、不触发个人闪避判定。
  - **无视穿透与 Armor=0**：屏障自身无防具减伤（`Armor=0`）全额抵扣折算后的伤害，即使敌方带有 `[穿透]` 标签也无法绕过屏障打在英雄生命值上。
  - **群攻仅扣一次**：屏障作为单个实体，敌方群攻技能仅命中扣减 1 次屏障数值，溢出部分平铺泄漏给全体英雄。
  - **负面隔离与视觉演出**：屏障存在时全额隔离敌方非伤害 Debuff，跳过受击反应弹窗。受击时触发蜂窝能量网格过载高亮与强震屏，被打破时触发红闪碎裂效果。
- **战术撤退（V6.12）**：
  - **单独按钮化**：撤离已从右上角移入右侧行动面板，收纳在 **【📦 基础行动】** 二级菜单中的 **【💨 尝试撤离】**（仅在我方角色行动回合可用）。
  - **对抗判定**：点击后统计敌我双方**实时平均速度**，做一次对抗 roll：`Random(1~我方均速)` vs `Random(1~敌方均速)`，**我方结果更大才成功撤离**。失败消耗本回合；成功弹出战后结算面板并输出全量消耗品库存供酒馆续写。
- **📜 战局记录预览与发送（V6.30 / V10.5 回合折叠）**：右上角固定 **【记录】** 按钮，点击唤起高透战术目镜记录弹窗：
  - **纯 CSS 回合折叠（V10.5）**：战局记录采用 `<details class="history-round-fold">` 纯 CSS 原生折叠结构按回合自动分组，**仅最新回合默认展开**，历史回合默认收折为 `ROUND XX` 胶囊标题栏（显示条目数与旋转箭头），点击即展开收起；零新增 JS 状态变量与事件监听，杜绝状态管理隐患，原始记录文本导出完全隔离不受影响。
  - **【🔓 解锁编辑】**：记录默认锁定只读，点击解锁后可直接微调战局文本，再点可锁回；编辑内容仅用于本次发送。
  - **【📤 结束当前战斗，将过程发送到酒馆】**：将当前记录原样注入酒馆聊天输入框，不生成额外续写旁白，玩家随时自由掌控剧情收尾。
- **📜 战局记录不含世界书激活情况（V7.0）**：战斗记录/结算面板/发送到酒馆的内容中不再包含「📚 图鉴载入」「世界书载入完成」等系统元信息；世界书激活情况在【📚 世界书管理】窗口查看即可。
- **🎬 击杀结算动画与 2.0 倍专属音频升级（V8.1）**：我方击杀敌方时按**伤害类型**自动分流三套视觉演出与专属高能音效（同轴斜切·近战 / 次元裂隙·法术 / 芝加哥打字机·远程），并通过 Blob 预载 + Web Audio API `GainNode` 统一以 **2.0 倍硬件级音量增益** 播放。
- **🎙️ 我方英雄回合开局专属语音系统（V7.9）**：为埃利奥特、玛德琳、弗兰克、索恩配备原声语音库（4 角色 × 7 句），开局登场/半血绝境动态选句，2.0 倍硬件增益与 Blob 预载零延迟。
- **🔍 敌方详情面板现代化重构与 20% 高透光战术目镜（V8.0）**：20% 透光战术全息面板、左 5/12 右 7/12 双列排版、前后排与阶位徽章、高质感 3-Row 技能卡、19 种变种标签专属语义图标与发光呼吸动效、世界书图鉴档案大展开空间。
- **⚠️ 动态属性修改与全效编辑器（V10.1 / V10.5 物品区块）**：
  在战斗进行期间，随时可点击“角色编辑”或“技能编辑”动态修改 HP/MP、攻防速度、技能威力消耗与物品道具配置，即时应用到当前战场：
  - **🎛️ 现代化战术弹窗与阵营标签页**：区分我方英雄与敌方单位独立标签页；
  - **📂 多级手风琴折叠与指标胶囊看板**：全局设置、角色卡片、技能卡片、**物品道具卡片**（V10.5）独立分级折叠管理；
  - **🎒 物品道具结构化配置（V10.5）**：角色折叠内专属物品区块，支持消耗品/道具/弹药类别切换、数量/次数配置、属性/特效/三效果槽定义、稀有度开关（✦传奇/⬥高阶）、技能绑定下拉联动。
- **🎮 右侧行动与技能面板 UI（V10.5 四大模块重构）**：
  - 右下角行动面板尺寸锁定为 **`260px` 宽度 × `172px` 高度**，由原平铺按钮升级为 **四大功能模块主入口**：
    - **`📦 基础行动`**：点击平滑滑入二级菜单，收敛 **`⚔️ 普攻攻击`**、**`🛡️ 防御姿态`**、**`💨 尝试撤离`** 三大通用操作；
    - **`🗡️ 攻击技能`**：点击滑入攻击类技能专属面板（玫红主题），由系统按 `MENU_DAMAGE_TAGS` 伤害/控制标签自动分流；
    - **`✨ 辅助技能`**：点击滑入辅助技能专属面板（翡翠绿主题），收纳治疗/加防/护盾/加速/驱散/冲能等支援技能；自动过滤反应/看破/舍身等非主动施放技能；
    - **`🎒 物品道具`**（V10.5）：点击滑入物品道具使用面板（琥珀金主题），展示角色所持有的消耗品与道具，支持主动使用与材料消耗；
  - **三排技能卡片**：【技能名称/类型】+【效果/蓄力/命中】+【消耗/材料/限制/状态】分三排清晰展现，资源或材料不足时自动呈现灰色遮罩与警示。
  - **深蓝科技系蓄力菜单**：技能处于 `[延迟]` 蓄力中时，自动切换为深蓝风格的【继续蓄力 / 终止蓄力】面板。

---

## 3. 技能编辑器标签作用全解析


标签的配置格式一般为：`[标签名;power:数值]`（例如 `[单体;power:30]`）。如果省略 `;power:xx`，系统会默认赋予其基础数值。**冒号简写（V10.6）**：效果槽标签也支持 `[标签名:数值]` 写法（如 `[单瞄:20]`），载入时自动归一为 `[单瞄;power:20]`——白名单仅覆盖效果槽标签，`[MP:8]`/`[Aim:120]`/`[数量:6]` 等自带冒号语义的字段不受影响。注意：同一串内的多个带 power 标签各自独立成槽，误写的无 power 标签不会再与后续标签合并。

### 3.1 伤害与攻击类标签
- `[单体]`：对单个敌方目标造成伤害。伤害计算公式：`技能实际威力 + (Buff攻击增益 * 爆发倍率)`。
- `[群攻]`：对敌方全体目标造成伤害。
- `[穿透]` / `[群穿透]`：造成的伤害将**无视目标的防御力(Armor)**，直接扣减生命值或护盾。
- `[眩晕]` / `[群眩晕]`：命中后对目标施加【眩晕】状态，使其**立即跳过下一次行动**。对多动单位（`Act > 1`）同样生效——其被眩晕后的下一次排轴行动（含额外行动）会被跳过，而非仅下回合开头。

> **💡 默认 Canvas 与 WebM 特效系统备注：**
> - **带括号的标签**：如 `[群攻(爆炸)]`、`[群攻(雷)]`、`[单体(枪击)]`、`[单体(火焰)]`、`[单体(光)]` 等依然可以使用。它们会触发对应样式的 Canvas 全屏粒子或默认刀光 SVG 动画。
> - **WebM 专属特效**：为了更加华丽的视觉演出，目前**更推荐使用 WebM 特效系统**。只需在技能中配置 `[特效:特效名]`（如 `[特效:火焰01]`），即可调用高清透明通道视频覆盖演出。

### 3.2 回护与增益类标签
- `[单回]` / `[群回]`：恢复生命值（HP）。圣职者或支援者使用时，溢出的治疗量会自动转化为护盾值。
- `[单回蓝]` / `[群回蓝]`：恢复法力值（MP）。
- `[单冲]` / `[群冲]`：恢复潜能值（TP）。`power` 数值即回复点数（如 `[单冲;power:30]` 恢复 30 点 TP），受 `[他人]` 目标选择修饰标签影响——`[单冲]` 带 `[他人]` 可对任意我方角色（含自己）施放，`[群冲]` 则对我方全体施放。
- `[单盾]` / `[群盾]`：为目标提供一层吸收对应数值伤害的护盾。
- `[肃正]` / `[肃正;power:数值]` / `[肃正:子类型;power:数值;Armor:N]`：**【全队共享伪实体肉盾屏障（V7.6/V10.0）】** 为全队生成共享圣域屏障（`teamBarrier`）。每次施放以本次数值为固定耐久上限；屏障存在期间所有【肃正】技能灰卡禁用，击破后方可重新施放；按克制属性呈现金/蓝/银白主题配色。拦截单体索敌，全额抵挡 `[穿透]` 攻击；敌方群攻仅扣减 1 次屏障数值；全额隔离非伤害类 Debuff；屏障被击碎前我方英雄处于无敌保护状态。屏障名字跟随施放技能名；`Armor:N` 为屏障提供对所有伤害（含穿透）的固定护甲值（可在编辑器 `🛡️护甲(肃正)` 直接配置）；`:子类型`（近战/远程/法术，编辑器类型下拉已直接提供 `[肃正:近战]`、`[肃正:远程]`、`[肃正:法术]`）使屏障对该属性伤害额外 50% 减免（先于护甲扣除）；屏障被击破后耐久上限与护甲/克制属性重置。
- `[单增]` / `[群增]`：提升目标攻击力（Atk），持续数回合（默认3回合）。
- `[单防]` / `[群防]`：提升目标防御力（Def），持续数回合（默认3回合）。
- `[单速]` / `[群速]`：**【速度增减（V6.11）】** 提升目标行动速度。速度增益为**独立的临时速度 buff**（`{type:'spd'}`），不改变角色基础属性 `Speed`、不影响闪避机制（基础闪避值仍取自原始速度），**只作用于行动顺序排轴**。`[单速]` 对我方单体施放（带 `[他人]` 可指定目标）、`[群速]` 对我方全体。施放瞬间**实时重排当前回合剩余队列**（更快者提前行动），且下一回合起由排轴自然生效；施法者已行动完，**不因加速额外获得一次行动**（除非技能带 `[再动]`）。
- `[单瞄]` / `[群瞄]`：提升目标命中率（Hit），持续数回合（默认3回合）。
- `[免伤]`：**【注意：这是单次消耗性的】** 为自身预存一次性减伤。在下一次遭受任何攻击时，按该数值临时提升防御进行抵挡，**无论是否成功抵挡，结算后立即被消耗清除**（与 `[回避]` 同属"单次消耗生效即消失"的机制）。
- `[他人]`：**【目标选择修饰标签】** 单独附加在技能上（类似 `[看破]`/`[反应]` 的独立标记，不占用技能类型槽位）。只有带 `[他人]` 的**纯有益单体技能**才能进入"我方选目标"，可对**任意我方角色（含自己）**施放；不带 `[他人]` 的纯单体有益技能（加攻/加防/加盾/治疗/回蓝/免伤/回避等）只能对自己施放。
- `[驱散:N]` / `[群驱散:N]`：**【负面状态驱散（我方专属，V10.0 编辑器支持）】** 对我方目标各驱散 N 个负面 buff（如 `【伊西斯之拥】[群回;power:100][群驱散:1]` 为全体回血 + 我方每人驱散 1 个负面；可在编辑器 `驱散量` 输入框直接配置 N）。`N` 可选，无数字默认 1（如 `[驱散]`、`[群驱散:3]`）。可驱散的目标负面状态：中毒、燃烧、眩晕，以及负面的降防/降准/降避/降攻；无负面时显示"无负面"占位反馈。`[驱散]` 视同 `[单增]` 类**纯增益单体**标签——不带 `[他人]` 只能对自己释放，带 `[他人]` 可对任意我方角色驱散；`[群驱散]` 则对我方全体释放。
- `[延迟:N]`：**【蓄力等待（我方专属）】** 技能先"蓄力"N 回合（该英雄处于 `⏳蓄力中`）。**资源（MP/HP/TP）在点击开启蓄力时即时校验扣除（押注资源）**；蓄力期间只处于准备态、不结算任何技能效果，但**仍可使用反应技能与看破**（不可主动普攻/技能/防御）。蓄力期间右侧技能面板切换为固定的两个选项：**【1.继续蓄力 2.终止蓄力】**；蓄力完成后面板变为 **【1.释放蓄力 2.终止蓄力】**，点击"释放蓄力"后若技能满足选目标条件（敌方单体/带 `[他人]` 的我方单体），则进入目标选择，否则直接释放。释放时不再重复扣除资源，并触发该技能全部效果（含自身携带的 `[加速:N]`）。
- `[加速:N]`：**【永久加速库存（我方专属）】** 一种不自动过期的加速点数。**只在蓄力释放结算完成时**才施加到角色身上；之后每次蓄力按"所需延迟回合"抵扣库存（蓄力3 遇 加速9 → 加速变6）。作为普通技能效果在施法时统一结算，可与其它标签（如 `[回避]`）同时触发；若技能带 `[他人]` 且所选目标为我方角色，则加速施加到该目标，否则施加到施法者自身。可为独立增益技能（直接+库存），也可与 `[延迟]` 同技能（如 `[延迟:3][加速:3]` 模拟"狙击枪3回合拼装后持续射击"：第一次蓄力3回合，完成后+3库存，**下一次**再使用即立即释放）。加速永远不影响"本次这一次"，只影响"之后"。
- `[限N次]`：**【本场战斗次数限制（我方专属）】** 限制某技能**本场战斗**内可使用的总次数（如 `[限1次]`、`[限3次]`）。累计用尽后：
  - **主动技能**：技能栏中该技能变为**灰色不可选中**，并显示 `[用尽]` 徽章；尚有余量时显示剩余次数 `[限N]`。
  - **反应/看破技能**（本就不显示在主动面板）：用尽后**后台静默失效**，不再出现在反应/看破弹窗候选；但弹窗中的技能按钮会**备注剩余次数**（如 `[限2次]`）。
  - 仅作用于**我方角色**，敌方 AI 技能选择不受此标签影响。
  - 次数为**本场战斗**内累计，战斗重置/新战局后自动恢复满值。

### 3.3 减益与控制类标签
- `[单降]` / `[群降]`：降低目标防御力（Def），持续数回合（默认3回合）。
- `[单盲]` / `[群盲]`：降准。降低目标命中率（Hit），持续数回合（默认3回合）。
- `[单缓]` / `[群缓]`：**【速度减速（V6.11）】** 降低目标行动速度。同 `[单速]/[群速]`，为**独立的临时速度 buff**，不改变基础 `Speed`、不影响闪避，只作用于行动顺序排轴；施放瞬间**实时重排当前回合剩余队列**（更慢者行动延后）。作为敌方减益随命中判定（有攻击标签时一次命中即施加全部妨害，纯减速时走完整闪避判定），接 `[必中]` 时强制命中。`[单缓]` 对敌方单体施放、`[群缓]` 对敌方全体。
- `[单滞]` / `[群滞]`：降避。降低目标闪避率（Eva），持续数回合（默认3回合）。
- `[单弱]` / `[群弱]`：降攻。降低目标攻击力（Atk），持续数回合（默认3回合）。
- `[中毒]` / `[群中毒]`：DoT伤害(毒)。每回合造成固定真实伤害，**叠层加深**，每次施放直接叠加新毒层，各层独立发作、互不覆盖，默认持续3回合。
- `[燃烧]` / `[群燃烧]`：DoT伤害(火)。每回合造成固定真实伤害，**灼烧禁疗**，重复施放不叠层、仅刷新强度与持续时间，且燃烧期间目标受到的 HP 治疗效果**减半**，默认持续3回合。
- `[吸蓝]`（V10.6）：**法力掠夺**。只能对**单一敌方目标**使用，`power` 为抽取点数（如 `[吸蓝;power:5]`）——**命中后**从目标身上抽走最多 5 点 MP 转加给施法者（目标剩余 MP 不足时按实际抽取，施法者以自身上限封顶）。走减益命中管线：作为纯吸蓝技能独立掷命中骰，与攻击标签同技能时共享该次攻击的命中结论，被闪避则整个抽取落空；目标无法力时反馈“无法力可抽”。受 `[必中]` 与 `[暴击]` 修饰，敌我双方、技能与物品通用。仅单体形态（无群吸蓝）。

### 3.4 机动与机制类标签
- `[必中]`：**【命中强化修饰标签】** 独立标记，不占用技能类型槽位。带此标签的技能跳过命中/闪避判定，**伤害与敌方非伤害妨害效果恒命中**（大范围妨害拟真）。详细规则见 §5 第 4 点。
- `[暴击:N%]`（V10.6）：**【暴击修饰标签】** 独立数值标记，不占用技能类型槽位（`%` 可省略，如 `[暴击:10]` 等价 `[暴击:10%]`，数值钳制 0-100）。技能/物品结算时按 N% 概率触发**与【极限爆发】完全相同的效果翻倍**——本技能所有效果槽（伤害/治疗/护盾/DOT/buff/debuff 数值）的 power 全部 ×2，战报标注 `暴击!`，施法者卡片播放金色“暴击!”飘字。**敌我双方、技能与物品、召唤/蓄力/反应触发全通用**（普攻除外）。与极限爆发的交互规则：
  - **只翻倍，不继承必中**：爆发翻倍附带“本次伤害与减益必中”，暴击翻倍不附带（仍可被闪避/反应化解）；
  - **与极限爆发互斥**：爆发回合 `multiplier` 已为 2，暴击掷骰直接跳过——**不会出现 ×4**，也不会重复飘字；
  - 暴击是白得的概率增益，不影响常规 TP 循环（照常 +5TP）。
- `[嘲讽]`：调整自身的嘲讽值（仇恨权重）。数值为正时吸引火力，数值为负数时则为“隐匿”。
- `[再动]`：使目标立即获得额外的行动回合。带有再动标签的技能**每回合我方限制使用一次**。
- `[反击]` / `[群反击]`：使自身进入反击架势。当成功闪避近战伤害时，会自动使用基础攻击力 + 反击威力的力量回敬对手。
- `[回避]`：**【注意：这是单次消耗性的】** 提升自身闪避。在下一次遭遇任何敌方攻击判定时起效，**一旦判定完成（无论是否闪避成功），该 Buff 立刻被消耗清除**。
- `[单避]` / `[群避]`：**【注意：这是持续性的】** 提升自身闪避，该 Buff 会在指定的持续回合内一直生效，**期间遭受多次攻击也不会消失**。

### 3.5 伤害属性标签（辅助判断）
- `[近战]`、`[远程]`、`[法术]`：定义技能的伤害属性。例如，防守者的反击技能只能对 `[近战]` 伤害进行判定。
- **技能面板显示规则**：右下角技能选择面板**仅在技能带显式** `[近战]/[远程]/[法术]` 标签时才显示类型徽章；未显式标注类型的技能（编辑器类型下拉选「无(近战)」）在面板上不显示类型徽章，但**内部结算仍默认按近战处理**（作为敌方动态技能的兜底策略）。

### 3.6 高阶/传奇叙事标签（V9.3，我方专属，无战斗效果）
- `[高阶]`：高稀有度，小人物与精英的分水岭；若为魔法，施法者即为高阶巫师。
- `[传奇]` / `[神术]`（`[神术]`为`[传奇]`别名，解析归一）：神话级，极少有人见过；若为魔法，可能是神明亲自赐予的神术。
- **类型修饰**：需与 `[近战]/[远程]/[法术]` 组合，决定旁白中“刀锋/枪火/咒文”的演出侧重。
- **旁白触发（仅本场首次，仅智慧敌，按最高阶位严格分镜）**：每技能**本场战斗仅首次使用/首次蓄力开始**触发一次特色旁白，释放者本人（遵循原“非1号位才说，1号位静默”规则）与**当前存活智慧敌中最高阶位**同场各说一句，程序先以 `getEnemyTier` 判定最高敌阶位（`legend>elite>fodder`，世界书等级直判优先、相对倍数兜底），再发送该阶位的**严格定位提示词**（杂兵必恐慌溃散、精英必忌惮凝重、首领必无畏审视，互斥）；后续同技能再次使用/持续蓄力/释放均回落为正常我方蓄力旁白（仅释放者本人一句，不再拉敌方互吹）。受「敌方旁白」与「友方旁白」开关分别守卫，`isRequesting` 单锁互斥。
- **编辑器**：我方技能卡勾选 `高阶/传奇` 复选框（传奇覆盖高阶），技能卡首排徽章 `⬥高阶(紫)/✦传奇(金)`，蓄力普通仅 start/continue 本人一句，蓄力高阶仅首个 start 走合并特色旁白。

### 3.7 看破战略中断标签
- `[看破:法术]`、`[看破:近战]`、`[看破:远程]`、`[看破:all]`：将该技能标记为看破技能，可在敌方宣告技能后、结算前尝试完全无效化对应伤害类型的技能。
- 看破技能也可以在技能编辑器中将“看破”下拉设为 `看破(全能)`、`看破(近战)`、`看破(远程)` 或 `看破(法术)`。
- **面板显示**：看破（`[看破]`）与反应（`[反应]`）类技能为**被动/打断触发机制**，不会出现在右下角主动技能选择面板中，仅当敌方宣告技能时由系统自动调用，无法被主动释放。

### 3.8 舍身护佑标签（V6.13）
- `[舍身]` / `[舍身:近战]` / `[舍身:远程]` / `[舍身:法术]`：将该技能标记为**舍身技能（我方专属）**，可在任意我方角色对敌方**单体**攻击闪避失败时，由其他角色挺身而出，替其承受本次攻击。
- **承接方式**：受击目标切换为舍身者，**跳过闪避（必中）**、伤害与附带减益**全盘接下**，后续减伤管线（屏障/防御/护盾/免伤/毅力留存/受击回气）对舍身者完整结算，原目标毫发无伤。
- **弹窗合并**：舍身候选与受击角色自身的**反应技能合并出现在同一弹窗**（无反应技能时单独弹出）；选择反应技仍可二次闪避"弹开"攻击；舍身者承接后**自身仍可释放反应技能**二次化解（但不可再连锁指定第三人舍身）。
- **优先级**：舍身在【看破】**之下**——看破在敌方技能宣告阶段（结算前）即拦截，成功则整个技能无效化；舍身需目标闪避失败后才触发。
- **限制**：仅对**单体**攻击生效（群攻不触发）；敌方不持有舍身技能；`[必中]` 不豁免舍身（必中仅跳过被动闪避，仍可被舍身/反应/看破主动化解）；屏障在场期间攻击被屏障归集、英雄不被选中，舍身天然不触发。
- **面板显示**：舍身（`[舍身]`）与看破/反应类技能同为**被动/打断触发机制**，不会出现在右下角主动技能选择面板中。

### 3.9 场地效果标签（V6.14）

**【场地】战前准备效果**：在 `<Combat_block>` 的 `我方列表` 行加后缀 `(场地:场地名)` 即可在战斗开始时为我方全体注入战前准备状态的 buff/debuff，合理反应跑团时战斗前的准备（如伏击、披甲、补给、地形等）。不带场地标签则无场地效果；场地效果**仅作用于我方**，敌方完全不受影响。

- **语法**：`我方列表(场地:有备而来):`（冒号支持半角 `:` 与全角 `：`）；未注册的场地名会被忽略并输出控制台警告（战斗正常建立）。
- **生效与衰减**：场地效果 = 战斗开始时给每个我方角色注入**真实 buff**（buff 图标正常显示在角色身上），衰减语义与全游戏「持续 N 回合」buff 一致——该角色**每回合首次行动**时消耗 1 次，**再动/多重施法/多动单位的额外行动不消耗场地回合**。
- **不可驱散**：场地 buff 带有 `fieldBuff` 标记，【驱散】技能**无法清除**（场地是战前环境状态，不是战斗中临时附加的 debuff）。
- **UI 展示**：右上角按钮区（【记录】按钮左侧）显示 🏟️ 场地徽章，鼠标悬浮可见效果描述；战斗开场战局日志写入 `🏟️ 场地：场地名 — 描述`。
- **重置保留**：场地效果在战斗开始时注入并随 `initialHeroesCache` 保存，**重置战斗后依然生效**（同一战局内反复重置均保留）。
- **【猝不及防】与反应技能**：场地闪避修正不影响反应技能——反应技的二次闪避判定只读取"本次反应新添加的回避值"，天然不受场地闪避增减影响。

| 场地名 | 效果 | 说明 |
| :--- | :--- | :--- |
| 有备而来 | 前三回合命中 +30 | 战前充分备战 |
| 猝不及防 | 第一回合闪避 -20 | 开场遭突袭，本人第一回合行动后即回过神来 |
| 背水一战 | 前三回合攻击 +15 | 退无可退，决死之志 |
| 严阵以待 | 前两回合防御 +15 | 列阵防御 |
| 居高临下 | 前两回合速度 +10 | 占据地形优势，仅作用于行动顺序（不影响闪避） |
| 劳师远征 | 前两回合速度 -10 | 长途跋涉疲惫，仅作用于行动顺序 |
| 雾霭弥漫 | 全场命中 -10 + 闪避 +10 | 雾气弥漫视野受限但便于隐蔽 |
| 士气高涨 | 开局全体 TP +20 | 战前动员（一次性，重置保留） |
| 补给充沛 | 开局全体 MP +30%（封顶上限） | 战前补给（一次性，重置保留） |
| 装备精良 | 开局全体护盾 = 最大生命 ×20% | 战前披甲（一次性，重置保留） |

### 3.10 敌方变种标签（V6.31）

**双写入点（通用）**：变种标签用 `[]` 包裹，可写在**名字**后（如 `名字: "食尸鬼[复活]"`），也可写在**属性栏**里（如 `属性: "[HP:100/100][复活]"`），**两处完全等价、任意混用**——解析端把两处的标签合并为同一命名空间，任一位置写入都生效，重复挂载幂等无害（布尔合并）。标签支持与 `*N` 数量后缀、世界书载入自由组合；从名字/属性栏中剥离后，`敌人.name` 与数值字段保持干净，标签不显示在战场上/战报/统计中。世界书模式下属性由图鉴词条提供、标签依然生效（写在 LLM 输出的名字上或词条属性栏里均可）。**未来新增任意标签均无需改解析代码**（属性栏与名字两处自动通吃）。

**全部标签均为敌方专属**（英雄无 tags 字段自动跳过）。已实现标签：

- **`[复活]`**：**【被动触发】** 带该标签的敌人**第一次**被击杀（HP 归 0）时，以 **30% 最大血量** 原地复活一次，随后正常回到战场与行动队列；仅一次（`reviveCount 1→0`），第二次被击杀即正常死亡。**覆盖所有死亡通道**：普攻/技能/群攻穿透/多段/反击/强力反击，以及毒/燃烧 DoT 在回合起始致死的场景——即使敌人在自己回合被反击杀死也会复活。
- **`[智慧]`**：与属性栏 `[智慧]` 完全等价，二者取并集（布尔合并，重复挂载幂等无害）。
  - **【战斗智能·残血威胁感知（V8.2）】**：带 `[智慧]` 标签的敌人在普攻、单体技能、减益技能与充能大招选择目标时无视普通嘲讽轮盘，**优先集火场上 HP ≤ 30% 的最残血英雄**（按当前生命百分比升序锁定最小者），形成致命斩杀合力；若场上有【肃正】屏障在场，屏障作为伪实体肉盾依然优先拦截（智慧集火无法绕过屏障）；当全体英雄 HP 均 > 30%（无残血目标）时，自动回落至标准嘲讽轮盘赌算法；非智慧敌人完全保持原有嘲讽轮盘不变。
  - **【三档旁白触发点】**：拥有三档独立台词触发（V6.26）：**回合开始**智慧旁白、**生命跌破 30%** 低血绝境台词（每次跌破仅触发一次）、**被击杀**临死台词；三者均受「敌方旁白」开关控制、均以本人口吻说话，详见 §2.1。详情弹窗名字旁显示智慧徽章。
- **`[流体]`** / **`[虚形]`** / **`[破法]`**：**【伤害类型减免】** 受到的**远程** / **近战** / **法术** 伤害各 **-50%**。作用于折算前原始伤害的独立减伤系数（与屏障克制同型），可与护甲/护盾/防御姿态叠加；【穿透】技能无视的是护甲、仍吃此属性减伤。*注：防守者【强力反击】为无类型上下文，不参与此减伤。*
- **`[防火]`** / **`[耐毒]`**：**【状态免疫】** 免疫**点燃** / **中毒** 施加（飘字「免疫·燃/免疫·毒」+ 战报）。
- **`[钢体]`**：**【状态免疫】** 免疫**眩晕**（飘字「免疫·眩晕」+ 战报）。
- **`[再生]`**：**【回合再生】** 每回合开始（TURN_START）回复 **最大 HP 10%**（上限不超过 maxHp）。
- **`[狂暴]`**：**【半血狂怒】** 当 **HP ≤ 30%** 时攻击力 **+50%**（肉鸽"半血狂怒"）。
- **`[吸血]`**：**【攻击吸血】** 对英雄造成**实际伤害**时，吸取其 **30%** 转化为自身回复（上限不超过 maxHp）。
- **`[蜕皮]`**：**【Boss 二阶段（V6.31）】** 首次 HP **跌破 50%** 时触发一次：**清除自身全部 debuff**（毒/燃/晕/降防/降准/降避/降攻）+ **回血 30%**（上限 maxHp）+ **永久攻击 +50%**（`molted` 标记 + 属性被动读取，不随回合过期、不可被驱散、跨【复活】仍永久）。受击后**一次阈值检查**、致死伤害不触发 → 一波斩杀可跳过二阶段，天然形成"斩杀线"决策。
- **`[荆棘]`**：**【近战反伤（V6.31）】** 被**近战**命中时反弹 **50% 实际伤害**（`AFTER_DAMAGE` 事件与【吸血】同管道），反伤走**正常减伤管线**——受攻击者护甲/护盾/防御姿态减免；**护甲减免后保底 10 点**（避免 -1 挠痒；防御姿态减半、护盾全额吸收照常）。反弹致命伤害可被我方保命被动（毅力留存/回避致命）救场；致死一击早退于事件 → **斩杀【荆棘】敌人不反弹**。
- **`[连动]`**：**【残血连动（V6.31）】** HP **≤ 30%** 时**立刻行动一次**（**一次性**，`linked` 标记防【再生】回血震荡反复触发）。与【再动】同款队列插队，由排轴正常拾取；DoT 致死不触发；入场即低血（场面上无跌破瞬间）时由回合开始事件兜底触发。
- **`[要害]`**：**【群攻减伤】** 受到的**群攻**技能伤害 **-50%**（含 `[群穿透]` 等一切群攻形态）。与 `[流体]/[虚形]/[破法]` 同型：作用于折算前原始伤害的独立减伤系数，可与护甲/护盾/防御姿态叠加；**单体**攻击不受影响（反击等单体近战不触发）。
- **`[自爆:群伤]`** / **`[自爆:群火]`** / **`[自爆:群毒]`** / **`[自爆:群穿]`**：**【死亡自爆】** 带该标签的敌人**真正死亡**时（第一次被击杀被 `[复活]` 拦截复活不自爆，复活后再死正常自爆）对**全体我方**结算一次完整技能管线：
  - 语法 `[自爆:群伤;power:N]`，`power` 缺省 **50**；`[自爆:群伤]`/`[自爆:群穿]` 的 power 即爆炸伤害（非穿透/穿透，穿透无视防御但仍吃护盾/屏障），`[自爆:群火]`/`[自爆:群毒]` 为纯状态（无直接伤害），power 即【燃烧】/【中毒】DoT 值（持续 3 回合，燃烧禁疗/中毒叠层语义全复用）。
  - **完整反制管线**：逐目标独立闪避、可触发我方反应拦截、可被【看破】打断（看破(全能)/(法术)）、【肃正】屏障吸收+泄漏、护盾/防御/保命被动正常结算——**先预警(600ms) 后看破判定，被看破则无爆炸完全无效化**。
  - **演出**：死亡时保留尸体 + 红色预警光晕 + `💥 自爆预警!` 飘字 → 爆炸粒子/震屏 → 结算完成后整卡消失（含血条/buff）；自爆不可被反击（攻击者已死亡）；多个自爆敌人同回合死亡顺序串行结算；击杀特写严格在自爆演出完全结束后播放。

> **示例**：`名字: "食尸鬼*2[复活][智慧]"` 或 `属性: "[HP:100/100][复活][智慧]"` → 载入 2 个「食尸鬼」，每个都带 [复活] 与 [智慧]，各可复活一次、各可触发智慧旁白。
> **组合示例**：`名字: "炎魔[防火][破法][再生][狂暴]"` → 免疫点燃、法术伤害减半、每回合回血 10%、残血时攻击暴涨。
> **Boss/精英组合（V6.31）**：`名字: "荆棘兽王[蜕皮][荆棘][连动][狂暴]"` → 首次跌破 50% 清状态回血并永久强化、近战反弹 50%（保底 10）、≤30% 立刻行动一次且残血攻击暴涨；`名字: "剧毒花妖[蜕皮][耐毒][再生]"` → 免疫中毒 + 二阶段净化 + 回合再生。
> **自爆组合**：`名字: "爆弹魔[复活][自爆:群火;power:40]"` → 第一次被击杀以 30% 血复活（不自爆），复活后再死才自爆，对全体我方施加 40 威力燃烧；`名字: "自爆傀儡*2[自爆:群穿;power:60]"` → 一次载入 2 个，各在死亡时对全体我方造成 60 威力穿透伤害（顺序串行结算）。
> **同名不同变种共存**：`【食尸鬼】` 与 `【食尸鬼[复活]】` 在同一列表中是两个独立敌人，互不合并、不报错。

### 3.11 召唤机制 [召唤:词条名]（V10.2）

**语法**：技能串中写 `[召唤:词条名]`（如 `【召唤·星界守卫】[召唤:星界守卫][MP:20]`），词条名按世界书图鉴匹配（剥离 emoji 后 `精确→别名→双向包含`），未命中或 `SIDE_LIMIT 4` 上限时飘字与战报提示失败。召唤与伤害/治疗可同技能共存：非纯召唤技能在召唤后继续结算 `tagList`。

- **即时参战与入队**：匹配成功后由开战同款 `parseEnemyItem→getEnemyTier→assignEnemyRows` 生成完整目标（继承世界书等级直判、变种 `tags/resist/immune/explode/revive/isWise` 与职业被动 `CLASS_PASSIVES.onBattleInit`），按 `actCount` 在当前行动者之后 `splice` 入 `actionQueue` 并 `reorderRemainingQueue→renderTurnQueue`，视觉走增量刷新（我方 `ally-summon-row` 追加/敌方 `renderEnemyFormation`），不全量重建闪场。召唤物 `SIDE_LIMIT` 按 `存活召唤数` 限 4，存档不污染。
- **侧别与敌我**：召唤目标记 `isSummon/side/summonOwnerId`，`side` 继承施法者侧；英雄侧召唤 AI 走 `executeSummonTurn`（复用 `selectEnemySkillAndTarget` 但敌我池按侧对调，`[回]/[增]` 对我方、`[单体]/[群攻]` 对敌方），敌方侧召唤走 `executeEnemyTurn`；`executeSkillAction` 的 `allies/opponents/targetIsAlly` 均按 `isSummon?side` 判定。
- **视觉与头像**：召唤与敌方同通道，有 `AVATAR_MAP` 用图否则走 `emoji`；战报与时间轴对召唤强制 emoji 通道，时间轴按 `ref.img` 真值判且 `onerror` 回落。
- **动画**：通过 `getEntitySpriteDom(s_*)` 双通道解析 `hero-card-s_* / s_*-sprite`，受击与攻击特效（`playSVGEffect/spawnHitFlash/shake`、蓄力 `spawnFocusOverlay/spawnChargeUpParticles`、连续扫射/近战/远程/WebM 快照）在召唤物上与开战敌人一致。

---

## 4. 独特的战斗机制

### 4.1 反应拦截机制 (Reaction)
反应机制是本系统最核心的博弈玩法，当角色遭受攻击时，可以通过消耗资源（MP/TP/HP）插队发动反制技能：
- **反应全能**：在技能编辑器中将反应类型设为 `[反应]` (或 `all`)。它可以应对**所有类型**的技能（无论是近战、远程还是法术伤害）。
  - *平衡建议*：由于全能反应能百分之百触发插队，属于极强机制，为了游戏平衡，**建议测试人员限制全能反应技能的数值强度，或大幅调高其 MP/TP 消耗**。
- **反应对应类型**：如 `[反应:近战]`、`[反应:远程]`、`[反应:法术]`。该反应技能只能在角色面临对应类型的伤害时才能激活。
- **执行逻辑**：
  在判定闪避失败、即将受伤前，系统会冻结时间轴。我方角色会弹出反应技能卡（敌方会自动智能选择）。如果角色选择使用反应技（例如使用了一个带有 `[回避]` 的身法反应），系统会在扣血前**临时增加回避值并重新进行二次闪避判定**，实现残血秀操作或完美格挡。
- **弹窗详情展示与伤害承伤预测（V8.6 增强）**：
  反应弹窗同步展示敌方本次攻击的**技能名、伤害属性（近战/远程/法术）、攻击类型（单体/群攻）、技能威力与命中率**，并于弹窗头部提供**逐段伤害与承伤预测卡片**（纯 debuff 技能不展示），为玩家插队决策提供精准数学参考：
  - **威力与增益段**：展示基础威力与攻击增减 Buff 对伤害的净影响（`⚔️ 威力 N [攻±X] → 原始 区间`，普攻显示攻击力）；
  - **防御与减甲段**：显示扣减目标有效防御（基础 Def + 护甲 Buff + 免伤）后的中间伤害区间（`🛡️ 有效防御 N → 区间`，防御姿态标注 `· 防御减半`，穿透标注 `⚡ 穿透·无视护甲`）；
  - **护盾吸收与最终 HP 段**：目标带有护盾时，单独披露护盾吸收量并精确映射至最终扣减 HP 区间（`🛡️ 护盾吸收 N → HP 区间`）；
  - **全队屏障提示**：若场上有【肃正】屏障在场，同步提示屏障吸收量；
  - **穿透与致命高亮警示**：穿透攻击呈现**琥珀金边框**与 `⚡穿透` 标签；若敌方技能的最大浮动伤害（Max Dmg）大于等于目标当前剩余生命值，卡片自动切换为**深红脉冲边框**并高亮提示 **`💀 可能致命（剩余 HP N）`** 警示动画。
- **反应技能触发边界（V8.5 优化）**：
  - **免伤反应仅对攻击生效**：携带 `[免伤]` 标签的反应技能仅在面临实际攻击类技能（含单体/群攻/穿透/眩晕）时激活，敌方施放纯减益（Debuff）技能时不会误触发消耗；
  - **智慧敌人穿透节约**：`[智慧]` 敌人面临 `[穿透]` 攻击时，由于穿透天然无视免伤，智慧敌人不会浪费资源施放无效的免伤反应技（非智慧敌人保留本能反应）。
- **绑定材料联动（V10.6）**：反应技能绑定材料（道具/消耗品/弹药）时——**候选预筛**：任一所需材料存量不足（或 `[限N次]` 用尽）的反应技不再出现在弹窗候选中；**弹窗徽章**：条目实时显示材料需求与剩余量（如 `🎒 逆咒归源镜×1（剩3次）`）；**扣减与战报**：反应释放时照常扣减材料并输出按类别区分的消耗子行（文案见 §10.3）；**触发来源**：反应战报主行注明触发来源（`> ⚡ 预判危机！X 施展反应技【Y】（应对 Z 的【W】）`），方便回溯是谁的哪一招逼出了这次反应。看破/舍身路径的绑定消耗规则见 §4.3/§4.4。

### 4.2 多次行动与蓄力机制
- **多次行动 (Extra Actions)**：
  部分强力怪物或 Boss 在角色编辑里可以设置 `Act (行动次数) > 1`（例如每回合动 2-3 次）。
  为了防止高速连动怪完全霸占时间轴，系统在每回合排轴时，怪物的额外行动会进行**速度衰减**，公式为：`实际排轴速度 = 基础速度 - (当前行动序数 * 10)`。这给予了玩家插队换气的战术空间。
- **我方多动（V6.27）**：**我方角色同样支持多动**，与敌方共用同一套排轴/速度衰减/事件节流引擎。可在**角色编辑器**的「行动」框设置次数（或通过 YAML 属性栏 `[行动次数:N]`/`[Act:N]` 写入），值 >1 时该英雄每回合可行动 N 次；多动英雄行动时，行动面板顶部显示 `⏩ 剩余 N 次行动` 提示剩余行动次数。多动语义与敌方完全一致：蓄力推进 / DoT tick / 回合开始事件仅在**首次行动**触发、眩晕只吞当前一条行动、`[再动]`/祈愿插队不受影响。
- **蓄力大招机制 (Charge)**：
  如果怪物的技能配置了 `[Charge:数值]`（例如 `【灭世火雨】[Charge:3]`），则该技能平时处于封印状态，怪物每回合结束时会自动积攒 1 点充能。
  - 当充能攒满时，怪物的充能灯会全部点亮，并向玩家发出**蓄力大招警告**（Ultimate Warning）。
  - 在轮到怪物行动时，系统会锁死操作并播放华丽的**敌方大招特写过场动画 (Cut-in)**，随后倾泻毁灭性的技能效果，释放后大招充能归零重新积攒。

### 4.3 看破战略中断机制 (Kanpo)
看破是与反应系统不同的战略级反制机制：敌方宣告技能后、结算之前，系统会扫描我方持有匹配看破技能的角色，并弹出独立看破面板供玩家选择。
- **触发时机**：敌方技能宣告后、技能效果结算前。
- **匹配规则**：`看破(全能)` 可中断任意伤害类型；`看破(近战)`、`看破(远程)`、`看破(法术)` 只能匹配对应伤害类型。
- **效果**：使用看破后，敌方本次技能被完全无效化；敌方资源照常消耗，我方需支付技能自身的 MP/HP/TP 消耗。
- **弹窗详情展示**：看破弹窗会展示敌方技能的攻击类型（单体/群攻）、**威力与命中率**；若为**单体攻击**还会额外显示**攻击目标**（屏障拦截时显示屏障名字），让玩家能针对受击角色精准决策是否打断。
- **威力过滤（V7.2）**：可在「战斗全局设置」中开启看破过滤（默认开启，阈值 100）——敌方技能威力最大值 ≤ 阈值时跳过看破不弹窗，仅对高威力技能保留打断机会。过滤在候选扫描前生效，主链（敌方技能宣告）与自爆路径（敌方死亡自爆）两处一并拦截。
- **绑定材料联动（V10.6）**：看破技能绑定材料（道具/消耗品/弹药）时——候选阶段任一材料存量不足（或 `[限N次]` 用尽）即静默失效、不进看破弹窗；弹窗条目显示材料需求徽章（如 `🎒 逆咒归源镜×1（剩3次）`）；看破成功时**照常扣减材料**并在战报输出按类别区分的消耗子行（文案见 §10.3，此前看破走独立结算路径，绑定形同虚设）。美化视图（战局记录）中看破主行解析为独立行动卡，消耗子行挂在卡片下（琥珀色高亮），无需展开原文即可见。

### 4.4 舍身护佑机制 (Sheshen)（V6.13）
舍身是与看破/反应不同的**守护向**反制机制：当某我方角色即将被敌方**单体**攻击命中（闪避失败）时，持有匹配【舍身】技能的其他角色可以挺身而出，替其承受本次攻击。
- **触发时机**：目标闪避失败后（晚于看破的宣告阶段，优先级在看破之下）。
- **执行逻辑**：舍身候选与受击角色自身的反应技能合并弹出；选择舍身后，受击目标切换为舍身者，**跳过闪避判定（必中）**，伤害与附带减益**全盘接下**，减伤管线（屏障/防御/护盾/免伤/毅力留存）对舍身者完整结算，原目标毫发无伤。
- **二次化解**：舍身者承接攻击后，**仍可触发自身反应技能**（如身法 `[回避]`）做二次闪避判定将攻击弹开；但不可再连锁指定第三人舍身（防无限转移）。
- **群攻无效**：群攻不触发舍身（v1 简化，需要时另行扩展）。
- **绑定材料联动（V10.6）**：舍身技能绑定材料（道具/消耗品/弹药）时——候选阶段任一材料存量不足（或 `[限N次]` 用尽）即静默失效、不进合并弹窗；弹窗条目显示材料需求徽章；舍身生效时**照常扣减材料**并在战报输出按类别区分的消耗子行（文案见 §10.3，此前舍身走独立演出路径，绑定形同虚设）。

### 4.5 敌方法力循环与内置行动机制（V8.2）
为了解决敌方长期战斗空蓝后只能单调普攻的问题，系统为所有敌方单位内置了统一的法力循环与战术行动 AI：
- **低蓝自动回蓝（Low-MP Mana Recovery）**：
  - **触发阈值**：当敌方当前 MP 低于最大 MP 的 30%（即 `enemy.mp < Math.ceil(enemy.maxMp * 0.3)` 且 `enemy.mp < enemy.maxMp`）时触发；
  - **执行效果**：敌方消耗本次行动机会，立即恢复 **最大 MP 的 30%**（`Math.ceil(enemy.maxMp * 0.3)`，向上取整）；
  - **视听演出**：触发专属 `mpHeal` 音效、青色魔力流光动画（`playSVGEffect`）、`+N MP` 浮动飘字，并在战报中记录 `🔋 [敌人] 魔力匮乏，消耗本次行动恢复法力！`；
  - **优先级**：**次于充能大招**（当大招充能满格时仍优先释放毁天灭地的大招，大招未满时才进入回蓝判定）；
  - **多动兼容**：多动敌人（`Act > 1`）仅在当前次行动用于回蓝，后续排好的其他行动照常执行，严格遵循"消耗 1 次行动"语义。
- **智慧威胁感知残血集火（Wise Focus AI）**：
  - 拥有 `[智慧]` 标签的敌人具备高级战术决策能力，在释放普攻、单体伤害技能、减益技能与充能大招时，无视嘲讽值轮盘，**优先集火场上 HP ≤ 30% 的最残血英雄**，配合斩杀线形成集火压制；
  - 存在【肃正】全队共享屏障时，屏障伪实体肉盾特性依然优先拦截；当无残血英雄时，自动回落至普通嘲讽轮盘算法。

### 4.6 敌方随机变异系统（Random Mutation）
为了增加战局的策略多变性与肉鸽趣味性，系统支持敌方单位在战局建立时动态获取变种标签：
- **触发与范围**：在全局设置中开启「🧬 敌方随机变异」后生效，覆盖 YAML 列表载入与世界书图鉴载入两条路径。
- **独立概率判定与去重规则**：
  - **变异池**：包含 15 个普通标签（`[复活]`、`[智慧]`、`[流体]`、`[虚形]`、`[破法]`、`[要害]`、`[防火]`、`[耐毒]`、`[钢体]`、`[再生]`、`[狂暴]`、`[吸血]`、`[蜕皮]`、`[荆棘]`、`[连动]`）与自爆互斥组（`[自爆:群伤/群火/群毒/群穿]`，四形态互斥，最多赋予一个）。每个标签按设定的独立概率（默认 1%）分别独立 roll。
  - **智能去重**：敌人已原生携带的变种标签自动跳过，同名标签绝不重复挂载；若敌人已带任一自爆标签，则自爆互斥组整体跳过。
- **纯视觉演出与战报零污染**：
  - **开场变身动效**：战斗开始敌人入场时，变异单位播放金色光柱扩散与金色脉冲闪光（`mutation-transform` + `spawnBurstAura`），并弹出金色「变异」浮动飘字；
  - **战报纯净保证**：变异过程**绝不写入战局记录（battleHistory）**，发送至酒馆的内容中零「变异」元信息字样，完美契合克苏鲁等各类跑团世界观叙事；变异特性由玩家直观观察开场变身动效及敌方详情弹窗徽章获知。

### 4.7 敌方前后排棋盘战术系统 (Front & Back Rows Formation)（V10.0）

为进一步丰富战斗策略纵深与阵型博弈，系统引入了敌方**前后排棋盘战术系统**，通过视觉立体景深与严谨的攻防掩护机制构建战场空间层次：

- **阵型划分与立体视觉景深**：
  - **后排（Back Row ⬆）**：视觉排布于上方，尺寸缩放为 `0.68x`（按屏宽自适应下潜至 `0.55x`），叠加 `brightness(0.82) saturate(0.92)` 景深暗淡滤镜，层级 `z-index: 10`；
  - **前排（Front Row ⬇）**：视觉排布于下方，保持 `1.0x` 标准近景比例，层级 `z-index: 20`，配合 `translateY(48px)` 纵向错位呈现强烈的立体进深感；
  - **自适应 2 行收敛约束**：无论是在宽屏桌面、酒馆侧栏挤压还是移动端竖屏，算法均基于战场可用宽度（`availW`）动态压缩间距与缩放，**视觉严格固定为 2 行（后排在上、前排在下）**，杜绝多敌人时换行拆解为 3~4 行的凌乱排版。
- **智能阶位与确定性分排算法 (`assignEnemyRows`)**：
  - **阶位评定 (`getEnemyTier`)**：系统优先根据世界书词条等级标签（如神话/首领/精英）进行直判；无等级时根据面板相对倍数与精英机制（`[复活]`、`[坚韧]`、`[再生]`、`Act≥2` 等）动态评定为 `legend`（首领，权重 2）、`elite`（精英，权重 1）、`fodder`（杂兵，权重 0）；
  - **分排规则**：支持 **手动指定**——名字或属性栏写入 `[前排]`/`[后排]` 双写入点等价（同串命中后排优先、`_manualRow` 记忆），**严格尊重手动溢出不纠正**；剩余空位中最高阶单位**锚定后排居中**（`ceil(nBack/2)` 位），其余按输入序补齐至 $\lceil N/2 \rceil$ 并写入 `_rowOrder` 供渲染排序；敌方召唤未手动则默认前排（`_rowOrder=999`）；
  - **严格幂等防震荡**：去除了所有随机构造，已全量含 `_rowOrder` 则不再重排（新召唤缺 `_rowOrder` 才触发），渲染时后排/前排分别按 `_rowOrder` 排序，彻底解决编辑器保存后前后排反复横跳闪烁与召唤盖掉手动布局的问题。
- **前排掩护机制（阻挡近战）**：
  只要敌方场上存在**存活的前排单位**（`hasAliveFront()`）：
  - **单体近战拦截**：玩家对我方英雄下达**普通攻击**、**近战单体技能**或释放**近战蓄力技能**指令时，若尝试点击后排敌人，系统立即弹出 `前排未清，近战无法突进后排` 战术提示并自动回退至技能选择界面，**不消耗当前回合与行动点**；
  - **近战群攻掩护**：施放伤害类型为 `[近战]` 的群攻技能时，目标集被前排阵型强行截断，**仅命中存活的前排敌人**（后排被前排肉身完全掩护），战报同步记录 `↳ 前排掩护：近战群攻仅命中前排`；
  - **近战保底随机**：未指定目标触发保底随机攻击时，近战攻击的随机候选池仅在前排存活敌人中抽取；
  - **前排全灭突进**：只有当敌方前排全部阵亡后，近战单位方可越过前线直接突进打击敌方后排。
- **后排掩护机制（削减命中）**：
  只要敌方场上存在**存活的后排单位**（`hasAliveBack()`）：
  - **远程/法术命中惩罚**：当我方英雄使用**远程**或**法术**技能攻击敌方**前排**单位时，受到敌方后排游弋火力与战术策应的干扰，攻击方命中上限受到基于目标敏捷的百分比削减：
    $$\Delta Hit = \lceil Speed_{target} \times 30\% \rceil$$
    $$Hit_{max}' = \max(1, Hit_{max} - \Delta Hit)$$
  - **战报与反馈**：战报清晰提示 `↳ 后排掩护：[施法者] 远程/法术命中 -[N]（[目标]敏捷[spd]×30%）`；
  - **敏捷对抗**：前排目标身手越敏捷（有效速度 $Speed_{target}$ 越高），在后排掩护下的规避与干扰收益越大。

---

## 5. 命中与闪避判定公式

系统的判定完全遵循以下数学公式：
1. **角色初始闪避值 = 角色的敏捷值 (即属性中的“速度 Speed”)**。
2. **初次判定（面板对抗）**
   - 攻击方的最大命中上限 $Hit_{max}$ = `基础100 (或技能Hit修正) + 攻击方命中增益Buff`
   - 防守方的最大闪避上限 $Eva_{max}$ = `防守方基础速度 (Speed) + 防守方闪避增益Buff`
   - 双方各自进行随机 Roll 点：
     $$Roll_{Atk} = Random(1 \sim Hit_{max})$$
     $$Roll_{Def} = Random(1 \sim Eva_{max})$$
   - 判断条件：若 $Roll_{Def} > Roll_{Atk}$，则闪避成功（显示“闪避”）；若小于或等于，则进入即将受伤阶段（或触发上述的反应拦截判定）。
3. **减益也受命中判定影响（V6.8 起，V9.1 修正为敌我一致）**：**敌我双方**的非伤害效果（降/盲/滞/弱/中毒/燃烧/缓）不再无条件生效，而是跟随**该技能的攻击命中**——**一个技能有多少次伤害攻击就判定多少次，只要有一次攻击命中，其携带的所有非伤害效果全部施加；闪避成功（含反应二次闪避）则整体落空**。
   - 例：`【砷弹射击】[单体(枪击);power:80][单滞;power:20][中毒;power:20][Aim:180][远程]` 判定 1 次，1 次命中即施加全部效果。
   - 例：`【"清算"三连发】[单体(枪击);power:80][单体(枪击);power:80][单体(枪击);power:80][Aim:100]` 判定 3 次（3 次独立攻击）。
   - 例：`【温切斯特独头弹】[单体(枪击);power:100][单体(枪击);power:100][单降;power:10][Aim:150]` 判定 2 次（2 次攻击）；只要其中 1 次命中，单降即施加。
   - 例（V9.1）：`【幽暗凝视】[单体;power:25][单盲;power:20][法术]` 攻击我方英雄时，英雄以 `【战斗本能】[回避;power:150][反应]` 闪避成功则 `单盲` 随攻击一同落空（V9.1 前因仅对敌方检查命中而被误施加，已修复）。
   - **纯减益技能**（无攻击标签、未带 `[必中]`）：整个技能仅做**一次**命中判定（对每个目标各 roll 一次），命中则该技能携带的全部非伤害效果一次性施加、闪避则整体落空。该判定走**与伤害攻击完全相同的命中管线**——包含反应拦截、闪避飘字/烟雾等演出与判定公式，命中不足时可正常触发目标反应技能。
   - **我方增益**（回血/加攻/加防/加盾/再动/肃正/驱散等）保持无条件生效，不受命中判定影响。
4. **`[必中]` 标签**：技能带 `[必中]` 时**跳过被动命中/闪避判定**，伤害与妨害效果恒命中（与 `[护甲]` 等在技能中并列书写即可）。即便目标被动闪避成功（如无伤害则跳过判定），也会施加 debuff，拟真大范围妨害无法**被动**闪避规避。**但 `[必中]` 不豁免反应技能/看破等主动化解**——目标仍可弹反应面板或看破打断（V6.10 起）。例：`【极地暴风雪】[群攻;power:150][群降;power:30][Charge:4][Aim:120][MP:4][法术][必中]`、`【拉莱耶造雾术】[群盲;power:20][再动;power:1][Turns:10][MP:2][法术][限1次][必中]`。
5. **极限爆发附加必中（V7.2）**：我方英雄激活「极限爆发」（消耗满槽 100 TP）后，本次攻击/技能的伤害与敌方减益**恒命中**——语义与 `[必中]` 完全一致（仅跳过被动闪避 roll，仍可被反应技/看破主动化解），无需在技能上书写 `[必中]`。仅我方生效；敌方满 TP 自动爆发不附加必中，判定维持原样。
6. **前后排掩护命中修正（V10.0）**：当敌方后排存活且我方攻击前排时，远程与法术技能的最大命中上限 $Hit_{max}$ 扣除后排掩护惩罚（$\Delta Hit = \lceil Speed_{target} \times 30\% \rceil$）。必中技能（`[必中]` / 极限爆发）跳过命中 roll 点，不受掩护惩罚影响。

> **✅ 已修复（V6.10）`[必中]` 不再跳过反应拦截**：敌方技能带 `[必中]` 时仍可触发我方角色反应技能（弹反应面板/看破打断）。`[必中]` 仅跳过**被动闪避 roll**，仍可被反应技/看破等**主动化解**。修复位置：`index.html` 反应拦截入口条件由 `!isDodged && !(skill && skill.guaranteedHit)` 改为 `!isDodged`。

---

## 6. 敌方 AI 索敌与决策机制

敌方攻击时的索敌目标遵循以下智能逻辑：
1. **优先保命与干扰**：如果自身 HP $\le$ 25%，优先对自己使用治疗或套盾；如果我方有英雄开启了“极限爆发”，优先对其使用降防/致盲等干扰技能。
2. **仇恨轮盘算法 (Taunt Roulette)**：
   当敌人使用单体伤害技能时，会扫描我方所有存活角色的“当前有效嘲讽值”（基础嘲讽值 + 嘲讽 Buff），并按嘲讽比例做成一个概率轮盘进行随机抽取。
   - **基础嘲讽值设定**：
     - **普通角色**：默认初始嘲讽值为 **100**。
     - **防守者**：因为天生防守被动，初始嘲讽值为 **200**（极其容易吸引火力）。
     - **隐匿者**：由于潜行被动，初始嘲讽值仅为 **50**（存在感极低）。
   - 这意味着防守者被怪物单体技能选中的概率是隐匿者的 **4倍**！

---

## 7. 七大职业与核心被动

职业在角色编辑界面的“职业”下拉框中选择，每个职业都拥有强力的专属被动：

1. **防守者**
   - **天生重装**：初始基础嘲讽值为 **200**。
   - **仇恨反击**：每次成功触发防守反击时，永久增加 20 点基础嘲讽值。
   - **盾牌精通**：将自身当前嘲讽值的 10% 转化为额外的命中率。
   - **【被动 - 强力反击】**：成功闪避攻击时有概率触发【强力反击】，以双倍攻击力（2 × Atk）重创目标（无视近战限制，不追加基础嘲讽），并伴有红色瞄准准心与全屏爆闪重击演出。触发几率随仇恨提升：基础 10%，每超出防守者天生仇恨（200）1 点 +0.2%（无上限）；打出一次强力反击后，仇恨回归天生 200 点，几率随之重置。**触发强力反击时不会同时触发普通反击**。
   - **【被动 - 毅力留存】**：每场战斗一次，承受致命伤害时强制保留 1 点 HP 避免死亡。
2. **狂战士**
   - **浴血奋战**：攻击力随损失的生命值比例线性提升（血越少攻击越高）。
   - **死斗**：当 HP 低于 30% 时，所有攻击附带【穿透】效果（无视防御），并额外造成自身已损失生命值 10% 的真实伤害。
   - **【被动 - 毅力留存】**：每场战斗一次，承受致命伤害时强制保留 1 点 HP 避免死亡（与防守者同款机制）。
3. **风行者**
   - **风之轻语**：面板闪避率（Eva）会额外增加当前的 TP（潜能）值。TP 越高，越难被击中。
   - **【被动 - 多重施法】（V7.1 转交）**：消耗 MP 释放技能时，有概率（根据耗蓝量决定，最高 30%）引发法力共鸣，立刻获得一个额外的行动回合（再动）。（原施法者被动转交风行者）
   - **【被动 - 回避致命】**：每场战斗一次，遭受致命伤害时完美回避免除伤害（与隐匿者同款机制）。
4. **灾厄使 / 施毒者**
   - **痛打落水狗**：目标身上每有一个负面状态（中毒、燃烧、眩晕、降防、盲目等），对其造成的伤害提升 15%。
   - **淬毒攻击**：每次攻击附加 3 回合的【中毒】状态，中毒伤害受自身攻击力加成。
5. **隐匿者**
   - **绝对潜行**：初始基础嘲讽值为 **50**。可以通过技能将嘲讽值降得更低。
   - **狙击 / 暗杀**：使用远程技能时，隐匿深度（100 - 当前嘲讽值）越深，额外伤害越高；使用近战技能时，隐匿深度越深，越大概率直接【眩晕】目标。
   - **【隐匿值累积】**：隐匿者通过行动累积"隐匿值"（初始 50，即 100 - 初始嘲讽 50）：
     - **击杀敌人 +30**、**一回合内未被敌方攻击命中 +10**（每回合结束统一结算；被敌方攻击命中——无论是否扣血、是否被护盾/屏障格挡、是否-0——当回合均不累积，只有本回合根本没被攻击打到才算"未受击"）。
     - **临时负 `[嘲]` 技能**（如 `【无迹可寻】[嘲讽;power:-30]`）也计入隐匿值，按数值累加（+30），默认持续 3 回合后自然消失、隐匿值回落。
     - **隐匿值 ≥ 80**：立刻触发 1 次【再动】（每循环一次；若隐匿值跌回 80 以下则解锁，可再次触发）。
     - **隐匿值 ≥ 100**：立刻获得下一次非反应攻击 **攻击力 +50**（单次消耗【参考回避/免伤】，默认持续 3 回合，一旦**真正的攻击**命中后便消失），随后隐匿值**回归 50** 开启新一轮循环。
     - **急袭保留规则**：若隐匿值是在施放某个隐匿/buff 技能时跨 100、由该技能自身触发急袭的，则急袭 buff **不会**被该技能瞬间吞噬，而是保留给下一次真正的攻击（攻/单体/穿透/眩晕标签）使用。
   - **【被动 - 回避致命】**：每场战斗一次，遭受致命伤害时完美回避免除伤害。
6. **施法者**
   - **法能护罩**：战斗开始时，自动获得相当于**最大 MP 50%** 的护盾值。
   - **【被动 - 法力回响】（V8.5 修正）**：施法或消耗 MP 时按 `概率 = min(100, mpSpent × 2)%` 进行**单次判定**（如消耗 30 MP 即 60% 概率触发一次，消除多骰叠加异常），命中则获得 **1 点永久加速库存**与 **15 点 TP**（上限封顶）。加速永久存在（仅被蓄力 `[延迟]` 抵扣消耗、不随回合过期）；TP 与全局施法 +5 TP 增量叠加（一次主动施法最高 +20 TP）。**所有消耗 MP 的途径均计算**：主动施法（含反应技）、蓄力押注、看破、舍身；蓄力押注触发的新加速在本次蓄力的 `effective` 计算之后落账——**不对本次蓄力生效**，仅服务后续蓄力。
7. **圣职者 / 支援者**
   - **神圣护盾**：治疗队友时，溢出的治疗量会自动转化为护盾。
   - **极限祈愿**：在满 TP 状态下点击其他队友，可以消耗全部 TP 发动祈愿，直接赋予该队友一次【再动】机会。

---

## 8. 如何自定义添加“角色头像”和“技能WebM特效”

### 8.1 自定义角色头像映射
在代码中搜索 `const AVATAR_MAP = {`，您会看到一个名字和链接的键值对。
- **添加方法**：在其中加入一行 `"角色名字": "图片URL地址"` 即可。
- *注：在角色编辑界面，您也可以直接在“头像”输入框中粘贴图片 URL。*

### 8.2 自定义 WebM 技能特效
在代码中搜索 `const WEBM_FX_REGISTRY = {`，注册您自己的透明通道 WebM 视频特效：
- **格式示例**：
  ```javascript
  '特效名称': { 
      url: 'WebM视频的URL链接', 
      scale: 1.4,              // 视频缩放比例
      particles: 'fire_aoe',   // 粒子背景类型
      audioUrl: '音效MP3链接'    // 该技能播放时专属的音效链接
  }
  ```
- 注册完成后，在技能编辑器中的 **“特效”** 下拉菜单中就会自动出现该特效名称，选中即可在施法时播放。

---

## 9. 世界书图鉴敌方数据载入系统（V6.15）

在 SillyTavern 酒馆环境中，可将**敌方图鉴世界书**（如 COC7 人类 NPC 战斗图鉴）作为敌方数据源：系统按敌方名字匹配世界书词条，从词条内嵌的 `<Combat_block>` 中精确载入该敌人的属性与技能。LLM 只需输出敌方名字，无需手写数值，避免摘录幻觉。

- **载入入口（准备页双按钮）**：准备页拆为 **【🧾 Yaml 载入】**（原逻辑，直接按 `<Combat_block>` 建局）与 **【📚 世界书载入】**（按名字匹配图鉴载入后建局）两个按钮；非酒馆助手环境自动隐藏世界书按钮，Yaml 载入流程完全不受影响。
- **世界书选择下拉（V6.16）**：世界书按钮下方带下拉框，可就地选择要载入的图鉴世界书（选中即绑定并持久化）。载入时**优先采用下拉选中值**；未选择时自动继承**角色卡绑定的世界书**（`getCharWorldbookNames('current')` 的 primary → additional[0]，兜底当前聊天文件绑定），绕开"绑定书里没怪物"的死锁；启动后自动绑定成功则后台预载词条，点击【世界书载入】即可直接使用。
- **匹配规则**：剥离名字 emoji 后按 ① 词条名精确 → ② 别名精确 → ③ 双向包含（≥2 字防误匹配）三级匹配（`resolveWorldbookEnemy`）。
- **载入行为**：
  - **命中**：提取词条内嵌 `<Combat_block>` 解析出敌人**属性/技能并完整替换**原数据（保留槽位 id），战局日志写入 `📚 图鉴载入：xxx → 条目「yyy」` 与命中统计。
  - **未命中**：保留 LLM 输出的原样数据（图鉴外临场新敌人不受影响）。
  - **全部未命中**：提示不开始战斗；读取失败/加载失败提示会**列出当前可用世界书清单**，便于核对名称与导入情况。
- **`*N` 数量后缀**：敌方名字加 `*N`（如 `❄️《冰雪随从》*2`）一次载入 N 个同名敌人（仅敌方列表生效，我方由编辑器/持久化维护）。
- **变种标签（V6.25）**：LLM 在敌人名字上加的 `[...]` 变种标签（如 `食尸鬼*2[复活][智慧]`）会在图鉴替换时**自动继承**——图鉴词条提供属性/技能，名字上的变种标签照样生效；标签也可直接写进图鉴词条的 `属性` 栏（双写入点等价）。当前支持 19 个标签见 §3.9。
- **词条背景描述自动提取与详情弹窗展示（V7.9）**：世界书载入敌人时，系统自动扫描并提取词条正文中的 `描述：`/`Description:`/`Desc:` 行文本（`extractWorldbookDescription`）；在战场中点击敌方头像查看详情时，变种标签下方将直接展示「📚 世界书资料」卡片，无需翻阅世界书即可直观查阅怪物来历与背景描述。
- **右上角【📚 世界书管理】弹窗**：分区块管理——**① 绑定世界书**（可搜索名称单选列表 + 🔄 刷新 + 绑定信息）；**② 词条列表**（搜索框 + 启用开关 + 启用词条置顶 + 「查看」词条原文）；**③ 当前载入敌人**（当前 `enemiesData` 摘要）；**④ 词条数据预览**（只读查看词条 content）。
- **持久化**：绑定世界书名与各词条启用开关存入 `localStorage`（`rpg_combat_worldbook_settings`），刷新页面后保留；管理弹窗打开时未绑定会自动绑定并拉取词条。

---

## 10. 物品道具与弹药消耗系统（V10.5）

为契合跑团、RPG 冒险中的道具管理与弹药资源限制，系统引入了完整的**物品道具与弹药消耗系统**，涵盖主动使用、技能材料消耗绑定、罕见首秀旁白与战后全量结算闭环：

### 10.1 物品数据结构与三大类别
在 `<Combat_block>` 中，角色可在 `属性:` 与 `技能:` 之间定义 `物品:` 列表（与 `技能:` 同级缩进在 `角色:` 内）：

```yaml
        物品:
          - "【芭斯特的猫眼石】[道具][高阶][单回;power:50][次数:3][他人]"
          - "【战斗兴奋剂】[消耗品][单回;power:60][单冲;power:20][数量:3][他人]"
          - "【.45手枪子弹】[弹药][数量:45][绑定:柯尔特左轮三连发*3;快速点射*1]"
```

- **三大类别标签（三选一）**：
  1. **`[消耗品]`**：药水、兴奋剂、投掷物等一次性消耗品。库存字段为 **`[数量:N]`**（缺省默认 1），战斗中使用或作为材料扣减后**跨场持久化减少**，耗尽即不可再用，战后结算会全量汇总剩余数量由 LLM 写入下一轮战局。
  2. **`[道具]`**：法宝、神器、便携装置等可重复使用的常备装备。库存字段为 **`[次数:N]`**（缺省默认 0 表示不限次数），每场战斗限制使用 N 次，**战斗重置或进入新战局时自动恢复满额**，战后不产生永久损耗（结算时不列入跨场消耗清单）。
  3. **`[弹药]`**：枪械子弹、特殊弹药等。库存字段为 **`[数量:N]`**，**不可在物品面板主动点击使用**（卡片置灰并标注 `技能弹药 · 不可主动使用`），专门通过 `[绑定:技能名*消耗]` 供特定枪械技能消耗。
- **效果槽位**：与技能完全同构，支持最多 3 个 `[标签;power:数值]` 槽位（如 `[单回;power:60][单冲;power:20]`），支持 `[近战]/[远程]/[法术]` 伤害属性、`[特效:xxx]` 特效名、`[持续:N]` 回合数与 `[他人]` 友方目标选择。
- **`[他人]` 目标取向**：带 `[他人]` 的有益物品可对我方任意指定队友施放；纯伤害类物品（如投掷炸弹）自动进入敌方目标选择；不带 `[他人]` 的有益物品仅能对自己生效。

### 10.2 战斗使用机制
- **行动消耗**：在右侧行动面板点击 **`🎒 物品道具`** 唤起物品列表，点击可用物品并选定目标即可使用。**使用物品将消耗当前角色的本回合行动**（与普攻/施法一致）。
- **`[再动]` 物品**：若物品效果包含 `[再动]`（如高能肾上腺素），使用后立即恢复行动权，天然形成“不消耗回合”的高级战术道具变体（每轮大回合我方限用一次）。
- **爆发与 TP 隔离**：使用物品不消耗 TP、不享受极限爆发翻倍增益，亦不积攒 TP 点数。
- **罕见物品敌我首秀合并旁白（`[高阶]` / `[传奇]` / `[神术]`）**：携带稀有度叙事标签的珍贵道具/消耗品，在**本场战斗首次使用时**自动触发一次全场特色合并旁白——使用者本人表达使用心境与底气，场上存活最高阶位的智慧敌方同步发表震撼/忌惮/审视台词，叙事词自动切换为物品专属分支（“刚刚使用了【XX】（高阶/传奇级罕见物品）”）。

### 10.3 技能材料与弹药绑定体系（`[绑定:技能名*消耗]`）
任何弹药、消耗品或道具均可通过 `[绑定:技能名*每次消耗;...]` 标签与我方技能建立材料关联（冒号支持半角与全角，多技能分号分隔，每次消耗缺省为 1）：
- **多技能与复合材料**：一个物品可绑定多个不同技能（如 `.45子弹` 绑定三连发消耗 3 发、快速点射消耗 1 发）；一个技能也可同时绑定多种材料（如需要消耗子弹 + 导引符咒，系统自动执行 AND 校验）。
- **技能卡材料看板与缺失警示**：绑定了材料的技能卡在技能选择面板中会实时渲染材料需求徽章（弹药 `🔹`、消耗品 `🧪`、道具 `🎒`，如 `🔹 .45子弹×3（剩42）`）。当任一所需材料库存不足时，技能卡自动灰显禁用并亮起红色 `⚠️ 弹药不足` 或 `⚠️ 材料不足` 警示。
- **扣减时机与战报文案（V10.6 按类别区分）**：
  - **即时技能**：技能释放瞬间扣除对应材料，战报追加消耗追踪子行。文案按物品类别区分且内嵌类别标签（战报供酒馆 LLM 续写下一份 `<Combat_block>`，必须杜绝歧义）：道具 **`↳ 使用道具：逆咒归源镜（道具·使用1次，剩1次）`**（强调可重复使用、只扣每场次数）；弹药 **`↳ 消耗弹药：.45子弹（弹药·消耗3发，剩42发）`**；消耗品或混合绑定 **`↳ 施展材料：铝热投剂（消耗品·消耗1个，剩2个）、.45子弹（弹药·消耗1发，剩44发）`**；
  - **蓄力技能**：技能开启 `[延迟]` 蓄力时即时押注扣除材料（与 MP 押注机制一致，中途取消或被击败不退还），蓄力完成释放时不重复扣减。
  - **反应/看破/舍身路径（V10.6）**：三类被动触发路径与主动技能同一套绑定语义——候选预筛（任一材料不足或限次用尽即不进弹窗）、弹窗材料徽章、触发时扣减与战报消耗子行全覆盖。看破/舍身为此前不扣材料的独立结算路径，V10.6 起补齐闭环。

### 10.4 战后全量结算与状态栏直连（V10.7）
战斗结束（获胜/失败/撤离）呼出结算面板时，系统执行两件事：
- **数值直连 MiniMapStatus 状态栏（程序写入，0 次 LLM）**：检测到酒馆助手变量接口且状态栏侧开启直连（默认开启）时，把战后各角色 HP/MP/存活状态与**消耗品与弹药**最终剩余量写入聊天变量 `$rpg_combat_result`（`$` 前缀对 LLM 隐形），状态栏侧程序按绝对值写入最新楼层状态数据——只改状态栏已有键与已有物品（不新增、未匹配项会在徽标中列明），弹药打空保留 `[名称：0]` 条目（供下一份 Combat_block 续带），HP/MP 上限沿用状态栏数值并在不一致时夹取+报告。写入完成后**合并层硬锁**生效：此后 `contextFloors` 楼层窗口内，状态栏更新 AI 针对这些已结算项的数值命令会被直接丢弃，防止战斗叙事引发二次扣减。
- **结算提示词改造**：直连成功时提示词不再包含数值结算清单与「更新状态栏」转述指令，改为程序换算的 `[小队战后伤势：]` 档位清单（无恙/轻伤/重伤/濒死/重伤濒死，MP 低于 25% 追加「法力耗竭」）与「数值已由系统写入、勿罗列数字」的指令；`<战局记录>` 全量战报仍原样发送（其自带【轻伤/受伤/重创】标签即「严格根据伤势标签描写」的数据来源）。直连不可用（独立浏览器模式或状态栏侧关闭直连）时自动回退为旧版「数值清单+转述指令」提示词。
- **同步徽标**：结算弹窗「小队战后状态」卡片上方显示同步状态——⏳ 已推送等待确认 / ✅ 已同步 N 名成员·M 项物品 / ⚠️ 已同步但存在未匹配角色·物品或上限不一致（展开明细）/ ⚪ 直连不可用已回退转述。
- **旧版全量清单**仍完整显示在结算弹窗的「小队战后状态」卡片中供玩家校对；道具类因每场重置不参与跨场结算。回退模式下，LLM 续写约定与旧版一致：下一份 `<Combat_block>` 的 `物品:` 列表按结算清单数量写回。
- **回归脚手架**：`integration-test/harness.html` 以 mock 酒馆环境装载两个应用并运行 41 项断言（端到端写入、幂等、上限夹取、零数量保留、未匹配上报、名称歧义、合并层硬锁、开关回落），可通过本地静态服务器访问验证。

### 10.5 全效编辑器物品道具配置
在战斗中呼出全效编辑器（“角色编辑”），展开任意我方角色卡片：
- **🎒 物品道具手风琴**：支持查看当前全部物品，点击「＋ 添加物品」即可就地新建；
- **全参数可视配置**：提供物品名称、类别下拉（消耗品/道具/弹药）、数量/次数数字框、伤害属性、特效动画、`[他人]` 友方开关、`[必中]` 开关、`[暴击%]` 概率框、三槽位效果标签与威力设定；
- **稀有度药丸开关**：提供 `✦传奇` 与 `⬥高阶` 互斥药丸按钮，保存时自动序列化；
- **技能绑定行**：自动列出该角色当前拥有的所有技能下拉菜单，支持自由添加/删除绑定行与配置每次消耗数量，改名或删除技能时系统控制台自动校验。

