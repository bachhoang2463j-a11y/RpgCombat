# 导演兜底人格 · 技术规格（交接文档）

> 本文档从 RpgCombat 会话提取，供独立对话实现。功能已从 RpgCombat 主线回退（基线 `b2e4a3b` = V10.11 + 3ce186c），此规格包含全部已验证的实现细节，可照抄落地或重新设计。

## 1. 功能定义

开战载入时检测**图鉴未命中且无技能**的敌人 → 弹确认窗（生成/跳过）→ 调用**独立 LLM 端点**按《敌方生成规则精简版》生成词条 → 程序校验 → 替换 `enemiesData` 槽位开战 → 写回图鉴（下次同名直接命中）。

**绝不死锁**：任何失败（端点未配/生成失败/校验不过）都回落"跳过"路径（敌人走普攻"猛击"兜底，现有引擎行为）。

## 2. 目标项目

`D:\Project\RpgCombat`（单 HTML 酒馆助手 iframe 战斗前端，`index.html` ~16000 行）。

**入口链**（已存在，不改）：
- `startGame()`（~:12359，Yaml 载入按钮触发）与 `startGameFromWorldbook()`（~:8211，世界书载入）开战前是兜底检测的插入点；
- `enemiesData` 敌人数组（无技能敌人现状：`selectEnemySkillAndTarget` 返回 null → 敌方走普攻 :13149 附近）；
- `resolveWorldbookEnemy(name)`（:7944）图鉴三级匹配（剥 emoji 精确→别名索引→双向包含）；
- `parseWorldbookEntryEnemy(entry)`（:8010）词条 content 提取 `<Combat_block>` 解析为 {名字,属性,技能}；
- `parseEnemyItem(data, index, tags)`（:7818）敌方对象构造（含变种标签/免疫/自爆全解析）；
- `parseAttributes(attrStr)`（:7453 附近）属性栏解析。

## 3. 设置入口：LLM 弹窗新 Tab

- 【💬 对话】设置弹窗（`#llm-settings-modal`，index.html:1862-1955）目前是单页无 Tab；
- 加 Tab 栏「💬 对话 | 🎬 导演兜底」（仿阵营编辑器 `switchPartyTab` :14301 的按钮组 + hidden 切换；注意 `.llm-tab-btn/.llm-tab-active` 样式需新加，之前写在 #llm-settings-panel 样式区 ~:981）；
- 兜底 Tab 复制 5 预设槽位机制（仿 `switchLLMPreset` :15948 + `defaultPreset` :15449 + `initLLMPresets` :15471）；
- 字段：独立 apiUrl / apiKey / model / **temperature（现有预设没有）** / max_tokens / 系统提示词 textarea；
- 系统提示词默认值 = 《敌方生成规则精简版》全文（`D:\Project\RpgCombat\敌方生成规则精简版.md`，150 行）。注意全文含 **139 个反引号**——不能用模板字符串内嵌，用 `JSON.stringify(md)` 生成字符串常量；
- 「↺ 恢复默认」按钮 + 独立持久化键 `rpg_combat_director_settings` **纯 localStorage**（不要走 TavernHelper setVariables——实测 character 变量通道不生效，S0 已删）；
- 保存按钮统一收集两 Tab（`saveLLMSettings` 里追加 `await saveDirectorSettings()`）。

## 4. 生成链路（全部已验证可行）

### 4.1 候选检测
```js
function detectDirectorCandidates() {
  return enemiesData.filter(e => {
    if (!e || !e.isAlive) return false;
    if (Array.isArray(e.skills) && e.skills.length > 0) return false;
    const entry = resolveWorldbookEnemy(e.name);
    if (entry && isWorldbookEntryEnabled(entry) && parseWorldbookEntryEnemy(entry)) return false;
    return true;
  });
}
```

### 4.2 精简请求函数（新写，勿复用 callLLMAPI）
现有 `callLLMAPI`（:16480）温度硬编码 0.85、系统提示词被战场快照包裹——不可用。新函数骨架（OpenAI 兼容 /chat/completions，非流式 fetch，Authorization Bearer，错误时 resp.text() 前 200 字符抛出）：

```js
async function callDirectorLLM(userContent) {
  const endpoint = directorState.apiUrl.replace(/\/$/, '') + '/chat/completions';
  const requestPayload = {
    model: directorState.model,
    messages: [
      { role: 'system', content: directorState.systemPrompt || DEFAULT_DIRECTOR_PROMPT },
      { role: 'user', content: userContent }
    ],
    max_tokens: directorState.maxTokens || 2048,
    temperature: (typeof directorState.temperature === 'number') ? directorState.temperature : 0.5
  };
  // fetch + resp.ok 检查 + data.choices[0].message.content 解析（兜底 reasoning_content/choice.text）
}
```

### 4.3 输入构造
一次请求补全全部候选：敌名列表 + 本楼正文片段（`getCurrentMessage()` 去掉 `<Combat_block>...</Combat_block>` 后取尾部 ~600 字符）。

### 4.4 输出解析
```js
function parseDirectorOutput(rawText) {
  // 提取所有 ```yaml 代码块（```(?:yaml)?\s*([\s\S]*?)```），
  // 每块 jsyaml.load → { 名字, 属性, 技能 }（无块时把全文当一个块试）
}
```

### 4.5 程序校验（精简版指南硬规则）
返回错误列表（空=通过）：
1. **标签白名单**（技能标签 44 个 + 变种群标签 17 个，不在列即拒）：
   - 技能：单体/群攻/穿透/群穿透/眩晕/群眩晕/单降/群降/单盲/群盲/单滞/群滞/单弱/群弱/中毒/群中毒/燃烧/群燃烧/吸蓝/必中/单增/群增/单防/群防/单速/群速/单回/群回/单盾/群盾/单回蓝/群回蓝/反击/群反击/Charge/Turns/回避/免伤/近战/远程/法术/Aim/MP/反应/枪击
   - 变种群：智慧/复活/流体/虚形/破法/要害/钢体/防火/耐毒/再生/狂暴/吸血/荆棘/连动/蜕皮/前排/后排（另有 `[自爆:群伤/群火/群毒/群穿]` 带子类型）
   - 提取法：`str.match(/\[([^\[\]]+)\]/g)` 后 `t.slice(1,-1).split(/[;:：]/)[0].trim()`
2. **Aim 落 100~200**（`\[Aim[:：](\d+)\]`）
3. **技能耗蓝 MP 总和 ≤ 属性栏 MP**（`parseAttributes` 可复用）
4. **技能数 ≤ 行动次数 + 1**
5. 变种群标签白名单（属性栏裸标签）

### 4.6 重试循环
失败带具体错误反馈（≤10 条）拼进下一轮 user content 重试，**≤2 次**；仍失败 → 回落跳过。

### 4.7 替换槽位
- 生成结果 `parseEnemyItem({名字,属性,技能}, 0, slot.nameTags)` → `fresh.id = slot.id` 保槽位；
- `enemiesData.map` 按 id 替换；`initialEnemiesCache` 重建；`assignEnemyRows()`。

## 5. 写回图鉴（TavernHelper API，已核实存在）

开发文档 `D:\Project\酒馆助手_TavernHelper_完整开发文档.md`：
- **`createWorldbookEntries(worldbook_name, new_entries, {render:'debounced'})`**（:3872，纯追加，返回 `{worldbook, new_entries}`）——优先用这个，不用 updateWorldbookWith；
- 词条 schema（:3507 WorldbookEntry）：`{ name, content, enabled:true, probability:100, strategy:{type:'selective', keys:[敌名]}, position:{order:100}, recursion:{prevent_incoming:true, prevent_outgoing:true} }`；
- content 格式：`<Combat_block>\n名字: "X"\n属性: "…"\n技能:\n  - "【…】…"\n</Combat_block>`（`parseWorldbookEntryEnemy` 可直接消费）；
- 写回后 `await loadWorldbookData()` 重载缓存使下次命中；
- **无绑定/非酒馆环境回落**：聊天变量 `insertOrAssignVariables({['$rpg_dynamic_bestiary']: {entries:[{name,content,created}]}}, {type:'chat'})` + 手动并入 worldbookCache（带 `_dynamic:true` 标记）+ buildWorldbookIndex；
- 【📚 世界书管理】弹窗（`renderWorldbookManager` :8515）可加动态词条区块（预览/删除单条）。

## 6. 触发点接线（两种方案）

方案 A（已验证可行）：`startGameFromWorldbook` 的 hitCount 检查后、`startGame()` 调用前 + Yaml 载入按钮 `onclick` 换成包装函数（`maybeStartDirectorFallback(() => startGame())`）。用原生 `confirm()` 弹窗即可（备战阶段无需花哨 UI）。

**重要教训：不要做战斗标记点（【战斗标记点】/{{战斗标记点}}短标记建局）**——实测与状态栏正则/酒馆 markdown 渲染管线存在不可调和的兼容性问题（详见 LOG-209 记录，已回退）。开战宣告就用极简 `<Combat_block>`（只写名字，技能由持久化 roster 补齐），这是验证过的工作形态。

## 7. 已验证的测试资产（harness test21，可参考）

`integration-test/harness.html` 的 mock 环境里：`window.fetch` 可 stub、`MOCK.worldbooks['测试角色卡']` 可放词条、`MOCK.api.getVariables({type:'chat'})` 可断言动态图鉴落库、`createWorldbookEntries` mock 已存在（:129）。四路断言：①生成过校验替换开战 ②必失败输出重试 2 次回落 ③动态图鉴落库且下次命中 ④禁用标签被拦截。

## 8. 流程约定

`node --check`（new Function 校验 script 块）→ harness/IAB 断言 → git 提交两步法（业务 commit → hash 回填 LOG → `docs(log): record`）。**注意**：在 addHistory 调用行之前插代码会使 test10 的 DIGEST_RULES srcLine 断言失配，需按序重映射重写（规则数 118，去重后按升序一一对应）。
